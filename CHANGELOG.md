# 變更紀錄

## 2026-08-14（新增：輸出貼紙PDF 依材質自動判斷「單面／雙面」版型）

### 新增（admin.html → NFC 名片量產管理 → 輸出貼紙PDF）

- **問題緣起**：使用者詢問「輸出貼紙PDF」是否已支援單面／雙面規格，實際檢查後發現這個功能原本並不存在——不管選哪一種貼紙材質，系統一律固定輸出「正面＋背面」兩頁，PVC 這種實際上只能單面印刷的材質也會多印出一頁沒用的背面。
- **規則確認**：跟使用者確認後，單面／雙面是由「貼紙材質」直接決定，不是額外的手動開關：
  - **PVC（白／黑）＝單面貼紙**：只印一頁，內容合併成 LOGO＋公司名稱＋NFC 字樣＋姓名／職稱＋QR Code。
  - **金屬(水晶貼)（黑／銀／黃金）＝雙面貼紙**：維持原本版型不變，正面是姓名／職稱／電話／Email／QR Code，背面是 LOGO＋公司名稱＋NFC 字樣。
- **修改內容**：
  1. `STICKER_OPTIONS` 5 種材質/顏色組合，各自新增 `sides`（`single`／`double`）欄位，PVC 固定 `single`、金屬固定 `double`；尚未設定材質/顏色的名片保守預設為 `double`（沿用原本版型，避免漏印內容）。
  2. `generateStickerPdf` 主流程改成依 `sides` 分流：PVC 呼叫新的 `drawStickerSingleSide`（單頁合併版型），金屬呼叫拆分出來的 `drawStickerDoubleFront`／`drawStickerDoubleBack`（維持原本兩頁版型）。
  3. 「設定貼紙材質/顏色」彈窗的材質卡片，各自加上「單面／雙面」小標籤；彈窗說明文字也補充規則說明，讓非技術人員一眼看懂。
  4. `stickerLabel()` 顯示文字（表格欄位／貼紙PDF 最後一頁的材質統計表／SVG 批次匯出的清單.csv）統一補上「（單面）」「（雙面）」註記，方便印刷廠商核對每種材質實際要印幾面。
- **影響範圍**：只調整貼紙 PDF 的版面配置與統計顯示文字；不影響向量 SVG 匯出（正面/反面各一檔的規則維持不變，屬於另一套獨立的印刷交付格式）、不影響任何資料庫欄位。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.19`，確保管理員打開 `admin.html` 時能拿到新版邏輯。

## 2026-08-14（調整：全站客服聯絡連結，從舊版 LINE 官方帳號改為「曜鼎科技客服中心」）

### 調整（card.html／edit.html／settings.html／yuyuko.html／faq.html）

- **問題緣起**：原本的客服聯絡管道是一個舊的 LINE 官方帳號（@632nedvu），使用者要求改成新的「曜鼎科技客服中心」官方帳號（@600kyyvp），並帶上預設訊息參數，方便使用者一點擊就能直接開啟對話視窗。
- **修改內容**：
  1. 把公開名片頁下方「如需申請實體 NFC 名片」提示區塊裡的客服連結（`card.html`、`edit.html`、`yuyuko.html`）、以及 App 設定頁「聯繫客服」項目（`settings.html`）、常見問題頁最下方的聯絡連結（`faq.html`），全部從舊連結 `https://line.me/R/ti/p/%40632nedvu` 換成新連結 `https://line.me/R/oaMessage/%40600kyyvp/?...`（帶預設招呼語參數）。
  2. 連結顯示文字統一從「LINE 官方帳號」改為「曜鼎科技客服中心」；帳號代號顯示從 `@632nedvu` 改為 `@600kyyvp`。
  3. 補上原本缺少的 `rel="noopener noreferrer"`，避免新分頁能反向操控原本頁面（資安基本防護）。
- **不影響範圍**：`edit.html` 裡「使用者自行填寫自己 LINE 官方帳號」的格式範例說明（`@632nedvu` 只是教學用的示範帳號，不是我們自己的客服帳號），維持原樣不動。
- **影響範圍**：只調整客服聯絡連結的網址與顯示文字，不影響任何資料庫欄位、名片資料結構或其他功能邏輯。

## 2026-08-13（調整：輸出貼紙PDF改為同時打包「向量SVG」，移除舊版逐人上傳向量LOGO功能）

### 調整（admin.html → NFC 名片量產管理 → 輸出貼紙）

- **問題緣起**：上一版做的「每位員工各自上傳一個向量 LOGO」功能，使用者後來反悔，認為要求每個員工一個個上傳不合理，要求整個刪除；同時使用者提供了實體金屬 NFC 名片的正反面照片（正面只有公司 LOGO＋NFC 圖示，反面是姓名／職稱／QR Code），要求輸出貼紙時能同時拿到「真正的向量檔」給印刷廠使用，並確認 LOGO 直接沿用電子名片編輯時已經上傳好、目前顯示在 `card.html` 卡片頂端的那個 LOGO 即可，不需要另外上傳。
- **移除項目**：
  1. 「NFC 名片量產管理」表格拿掉「列印用向量 LOGO」欄位與「上傳 SVG」按鈕。
  2. 移除對應的上傳邏輯（`triggerVectorLogoUpload`／`handleVectorLogoFileSelected`）、雲端寫入函式 `adminSetLogoVectorPath`，以及只為了把向量 LOGO 直接畫進 PDF 用的 `svg2pdf.js` 函式庫。
  3. 貼紙 PDF 背面 LOGO 繪製邏輯回歸原本單純的點陣圖版本。
- **新增功能**：
  1. 「🖨️ 輸出貼紙PDF」彈窗新增兩個勾選項：「貼紙 PDF 印刷稿」與「向量 SVG（正/反面各一檔，打包 ZIP）」，預設都勾選，可各自單獨產生。
  2. 正面 SVG：白卡底＋直接沿用該名員工名片上既有的 LOGO 圖片（沒有 LOGO 就留白，不中斷整批匯出）。
  3. 反面 SVG：姓名／職稱用 SVG 向量文字呈現，並內嵌一個「只含這張卡實際用到的字」的迷你字型檔（用 `opentype.js` 即時從完整的 11.9MB 中文字型檔裡挑字現做，每張卡只有幾 KB，不用整包塞進去）；QR Code 也改成真正的向量路徑（合併相鄰黑格成一條路徑，檔案更小），並補上先前沒有的掃描留白區（quiet zone），避免掃描率變差。
  4. 批次輸出的所有 SVG 檔，連同一份「清單.csv」（序號/姓名/公司/職稱/貼紙材質顏色/NFC ID/名片網址），用新引入的 `JSZip` 打包成一個 ZIP 一次下載。
- **已知限制**：正面 LOGO 本質上還是沿用既有的點陣圖素材（不是重新描邊的向量圖），如果印刷廠對 LOGO 本身的向量純度有硬性要求，這個做法仍無法滿足；反面文字用的是 SVG 內嵌字型技術，瀏覽器開啟顯示正常，但 Illustrator／Inkscape 開啟時可能不吃內嵌字型、改用替代字型，字距/字寬可能跑掉，這是這次刻意接受的簡化做法。
- **影響範圍**：只調整「輸出貼紙PDF」彈窗與其產生邏輯；不影響公開名片頁 LOGO 顯示、不影響既有的個人大頭貼/Logo 上傳流程、不新增任何資料庫欄位或 Storage 權限。
- **快取更新**：`admin.html` 內 `cloud.js` 版本參數升為 `?v=20260813c`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.18`。

## 2026-08-13（修正：淺色主題下部分圖示會消失，全站盤點修正）

### 修正（styles.css → 單色功能圖示）

- **問題緣起**：使用者回報套用某些名片主題後，「選擇聯絡方式類型」彈窗裡電話／Email／網站等圖示變得幾乎看不到，要求檢查全站是否有同樣問題並修正。
- **查出的原因**：電話／手機／Email／網站／檔案這幾個圖示（`phone-icon.svg`、`mobile-icon.svg`、`email-icon.svg`、`website-icon.svg`、`file-icon.svg`）都是寫死的純白色 SVG，用 `<img>` 標籤載入、沒辦法跟著頁面顏色變化。這些圖示原本只在深色背景上使用沒問題，但只要容器背景在「淺色主題」（`body.theme-light`，以及公開名片頁的「淺色主題／theme-2」「奶茶桃粉／theme-7」「珊瑚蜜桃／theme-9」「琥珀橙焰／theme-11」）變成白色/淺色，白圖示疊白背景就完全看不見。原本有嘗試用陰影補救，但陰影對「純白疊純白」這種情況幾乎沒有效果。額外發現 `twitter-logo.svg` 用了 `fill="currentColor"`（透過 `&lt;img&gt;` 載入抓不到顏色，瀏覽器預設變黑色），跟其他社群圖示的寫法不一致，會在深色背景上黑圖示看不見。
- **修正方式**：
  1. 把電話／手機／Email／網站／檔案／X 這 6 個單色圖示，統一改成「固定品牌綠圓形底色 + 白色圖案」，不管容器背景是深是淺都保證看得清楚，也比原本更醒目。
  2. `twitter-logo.svg` 改成跟其他社群圖示一致的寫死白色（`fill="#FFFFFF"`），不再依賴抓不到的 `currentColor`。
  3. 全站盤點後確認以下地方都有同樣的問題並一併修正：`edit.html`「選擇聯絡方式類型」彈窗、公開名片頁（`card.html`）聯絡按鈕（尤其是使用者自己編輯過名片後，所有聯絡方式按鈕都會變成同一種樣式，影響範圍比原本回報的更廣）、`directory.html`「＋新增好友 → 匯入聯絡人」的檔案選擇區塊。
  4. 確認 Facebook／Instagram／LinkedIn／YouTube／微信／WhatsApp／LINE 這些社群圖示本身就是彩色或自帶色底，不受這個問題影響，沒有更動。
- **影響範圍**：只調整這 6 個單色圖示的視覺呈現方式；不改任何主題的整體配色、不改按鈕文字顏色、不改資料儲存邏輯。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.17`，確保手機端能拿到新版 `styles.css` 與 `edit.html`。

## 2026-08-13（新增：手動聯絡人可「轉為正式名片」，才能出現在 NFC 名片量產管理）

### 新增（admin.html → 手動聯絡人待轉換）

- **問題緣起**：使用者用「+新增好友」新增聯絡人（陳佑圻）後，發現這個人沒有出現在 admin.html「NFC 名片量產管理」的名片列表裡。
- **原因說明**：「+新增好友」存的是使用者自己帳號底下的**私人聯絡人資料**（`directory_contacts`），設計上刻意跟「正式會員名片」（`cards`，有自己的 `card.html` 網址、任何人都能連到）分開——因為這是使用者自己輸入的別人的資料，對方本人並未同意公開，所以之前只能私下預覽、不會出現在管理員的名片列表裡。
- **這次調整**：確認使用者需要的是「把手動聯絡人正式升級成可以量產 NFC 的真實名片」，因此新增轉換機制：
  1. `admin.html` 最上方新增「手動聯絡人（待轉為正式名片）」區塊，列出自己新增過、還沒轉換的聯絡人，每筆都有「轉為正式名片」按鈕。
  2. 按下後會建立一張真正的會員名片（有自己的 `user_id`、公開 `card.html?id=...` 網址、可綁定 NFC），並自動清掉原本的私人聯絡人資料，避免同一個人重複出現兩筆；轉換後立即出現在下方名片管理列表，可以設定貼紙材質、列入批次量產。
  3. 轉換前會先用電話／Email 查重：如果這個人剛好已經是會員了，就不會重複建立，直接連回既有名片。
  4. 資料庫新增 `cards_admin_insert` 權限規則：原本管理員只能「修改/刪除」別人的名片，沒有「新增」別人名片的權限，這次補上（企業管理員仍只能建立自己公司的名片，超級管理員不限制）。
- **影響範圍**：只新增轉換功能與對應的資料庫寫入權限；不影響現有「+新增好友」私人預覽功能、不影響既有會員名片的顯示與編輯邏輯。
- **快取更新**：`admin.html` 內 `cloud.js` 版本參數升為 `?v=20260813b`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.16`。

## 2026-08-13（新增：NFC 名片量產管理可上傳「列印用向量 LOGO」，貼紙 PDF 自動改用向量內嵌）

### 新增（admin.html → NFC 名片量產管理）

- **問題緣起**：使用者要求「輸出貼紙PDF」裡的 LOGO 必須是向量檔（給印刷廠用），但目前系統不管上傳什麼格式的 LOGO，一律會被瀏覽器 canvas 轉成點陣圖（WebP/JPEG）存起來，貼紙 PDF 也是直接貼這張點陣圖，完全沒有保留向量資料——這件事 8/3 已經討論過一次並記錄在案：「技術上無法自動把已經是點陣圖的 LOGO 轉換成真正的向量圖」。
- **技術限制説明**：瀏覽器 JavaScript 沒辦法產生真正的 Adobe `.ai` 檔（那是 Adobe 自家的專屬格式）；業界通用、技術上可行的做法是改用「向量 PDF 內嵌」——把 SVG 向量路徑直接畫進 PDF，印刷廠或 Illustrator 開啟後一樣是完整向量、可以無限放大不模糊，效果等同 AI 檔，只是副檔名是 `.pdf`。
- **新增功能**：
  1. 「NFC 名片量產管理」表格新增「列印用向量 LOGO」欄位，管理員可以幫任何一位員工上傳真正的向量檔（只接受 `.svg`，這是瀏覽器原生、Illustrator 也能直接開啟編輯的向量格式），原始 SVG 內容直接存檔，不經過既有的 canvas 壓縮流程（那個流程一定會把圖轉成點陣圖）。
  2. 「輸出貼紙PDF」時，如果該員工有上傳向量 LOGO，會優先用 `svg2pdf.js` 把 SVG 向量路徑直接畫進 PDF；沒有上傳向量版，或向量版剛好讀取失敗，才退回原本的點陣圖版本，不會中斷整批匯出。
  3. PDF 最後一頁的統計表新增一行說明，列出這次匯出裡有幾張用了向量 LOGO、幾張還是點陣圖、幾張完全沒設定，方便直接回報印刷廠或知道還有哪些人需要補上傳。
- **影響範圍**：只新增 `profile_json.logoVectorPath` 這個獨立欄位與對應的上傳/讀取邏輯；完全不影響既有的個人大頭貼/Logo 上傳流程與公開名片頁顯示（`card.html` 上顯示的還是原本的點陣圖版本，向量版只用在貼紙 PDF 輸出）。
- **快取更新**：`admin.html` 內 `cloud.js` 版本參數升為 `?v=20260813a`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.15`。

## 2026-08-12（調整：通訊錄手動聯絡人列表跟真實會員顯示一致，刪除移到預覽頁）

### 調整（平台通訊錄 → 手動聯絡人列表 / 名片預覽）

- **問題緣起**：手動新增的聯絡人在通訊錄列表上多了一行「手動儲存聯絡人」說明文字跟「刪除」按鈕，跟其他真實會員的顯示方式不一致，看起來像跑版。
- **調整方式**：
  1. 列表項改成跟真實會員完全一樣：只有姓名、公司｜職稱、一顆「👀 預覽」按鈕，不再顯示額外說明文字。
  2. 「刪除聯絡人」移到預覽頁面（`card.html?contact=...`）裡面，點進去才會看到「🗑 刪除聯絡人」按鈕，刪除後導回通訊錄；列表上不再有刪除功能。
- **影響範圍**：只改通訊錄列表的顯示樣式與刪除功能的位置；不改資料儲存方式、不改公開名片邏輯。
- **快取更新**：`directory.html` 內 `directory-a.js` 版本參數升為 `?v=20260812c`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.14`。

## 2026-08-12（新增：手動聯絡人也能點「預覽」看到完整名片畫面）

### 新增（平台通訊錄 → ＋新增好友 → 預覽）

- **問題緣起**：手動新增的好友之前只能看電話／Email 按鈕，沒有像其他真實會員一樣的「預覽」名片畫面，使用者希望點下去也能看到跟其他人電子名片一樣的完整介面。
- **設計考量**：手動新增的聯絡人資料是「你自己輸入的別人的資料」，對方本人並不知情、也沒有同意公開——跟邀請好友自己填資料建立公開名片是完全不同的兩件事。所以這裡刻意做成**只有你自己登入後看得到的私人預覽**，不會產生任何人都能打開的公開名片網址，避免變成沒有對方同意就外流的個資。
- **實作方式**：
  1. 新增 `getMyContactById()` 雲端 API，一樣受 Row Level Security 限制，只能讀自己新增的資料。
  2. `card.html` 新增 `?contact=<id>` 預覽模式：需要先登入，並直接沿用跟其他公開名片完全一樣的排版與程式邏輯（姓名、職稱、公司、電話／Email 按鈕），沒有的欄位（大頭照、標語、公司驗證資訊等）就自然不顯示；「分享名片」「留下資料」等對第三方聯絡人沒有意義的功能整組隱藏。
  3. 通訊錄列表裡手動聯絡人的按鈕改成「👀 預覽」＋「刪除」，跟其他真實會員的顯示方式一致。
- **影響範圍**：只新增 `card.html?contact=...` 這個私人預覽模式；不改公開 `card.html?id=...` / `card.html?nfc=...` 既有邏輯與畫面（已實測確認正常）、不建立任何新的公開名片資料、不影響邀請好友或訪客建立名片的流程。
- **快取更新**：`directory.html`／`card.html` 內 `cloud.js` 版本參數升為 `?v=20260812b`；`directory.html` 內 `directory-a.js` 版本參數升為 `?v=20260812b`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.13`。

## 2026-08-12（修正：「＋新增好友」在正式環境仍然新增失敗）

### 修正（Supabase 資料庫 → directory_contacts）

- **問題緣起**：把「＋新增好友」接上雲端資料庫後，實測發現送出仍然顯示「新增好友失敗，請確認已登入後再試一次」，即使確定已經登入。
- **查到的原因**：`directory_contacts` 資料表的 `owner_user_id` 欄位還留著一條「必須是 Supabase 內建帳號」的外鍵限制（`references auth.users(id)`）。但這個平台的使用者幾乎都是用 LINE 登入（走自訂 JWT，不是 Supabase 原生帳號系統），使用者編號並不存在於 `auth.users` 這張表裡，所以每一筆新增都會被資料庫直接擋下（外鍵違反錯誤 23503）。專案的資料庫設定腳本（`supabase-setup.sql`）裡其實已經寫了移除這條限制的語句，但從未真正套用到正式資料庫。
- **修正方式**：新增 migration `20260812190000_directory_contacts_drop_auth_users_fk.sql`，正式移除這條外鍵限制，並已套用到正式資料庫；套用後實測用非 Supabase 帳號的編號寫入可以成功。同時確認同樣是給 LINE 登入使用的 `consents`（使用者同意紀錄）表沒有這個問題（限制已經在更早之前移除）。
- **影響範圍**：只調整 `directory_contacts` 資料表的外鍵限制；不改任何前端程式碼、不改其他資料表結構、不影響 Supabase 原生帳號（若未來啟用 Google/Apple 登入）使用者的行為。

## 2026-08-12（修正：手機用 LINE 登入一次後又跳回登入頁，要重複點擊才能進入）

### 修正（auth.html → LINE 登入）

- **問題緣起**：使用者回報手機瀏覽器（Safari／Chrome）按「登入」，第一次跑完 LINE 登入流程沒問題，卻又跳回我們自己網站的登入頁，要再點一次才能真正進入系統。
- **查到的原因**：手機上按「登入」後，網站原本會先自動把畫面切去「LINE App」裡完成登入（用意是讓使用者不用打帳號密碼）。但 LINE App 是一個完全獨立的瀏覽器環境，跟原本開著的 Safari／Chrome 分頁是「兩個不同的儲存空間」——登入成功的憑證只會寫進 LINE App 那一份，切回原本分頁後，那份分頁完全不知道使用者已經登入，還是顯示沒登入的畫面，使用者只好被迫重新點擊登入。程式碼裡其實已經處理過同一類問題（僅限於「加入手機主畫面」開啟的情況），但一般手機瀏覽器直接使用時沒有涵蓋到。
- **修正方式**：手機瀏覽器按「登入」後，統一改成跟電腦一樣導向 LINE 官方登入頁（`access.line.me`），不再由我們自己土法煉鋼「先切去 LINE App、沒反應再自動退回」。LINE 官方登入頁本身在手機上就會提供「用 LINE App 開啟」的官方選項，並保證登入完成後正確導回原本這個分頁，不會再發生「登入到另一個地方去」的問題。同時移除了原本用來偵測「切去 App 後有沒有卡住」的 2.2 秒計時器與逃生門連結——這個計時器判斷失準，正是造成使用者需要重複點擊的根本原因。
- **影響範圍**：只改 `auth.html` 與 `js/pages/auth/main.js` 的 LINE 登入按鈕行為與說明文字；不改 LINE 登入後端交換邏輯（`line-auth` Edge Function）、不改已經運作正常的「從 LINE App／LINE 聊天室內直接開啟」自動登入流程、不改電腦版 QR Code 登入。
- **快取更新**：`auth.html` 內 `js/pages/auth/main.js` 版本參數升為 `?v=20260812a`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.8`。

## 2026-08-12（新增：「＋新增好友」正式併入平台通訊錄，不再只存在瀏覽器本機）

### 新增／修正（平台通訊錄 → ＋新增好友）

- **問題緣起**：「平台通訊錄」的「＋新增好友」功能過去只是半成品——填完表單後資料只存進當前瀏覽器的 localStorage，換一台電腦、換一個瀏覽器、甚至只是清一下瀏覽器資料，新增的好友就整個消失，也不算真的「加入通訊錄」。
- **查到的關鍵**：資料庫其實老早就設計好一張專門給這個功能用的 `directory_contacts` 資料表（每人只能讀寫自己新增的資料，Row Level Security 已經設好），但前端程式碼從未真正接上這張表，一直停在「先用 localStorage 頂著」的階段。
- **這次改動**：
  1. 新增雲端 API：`getMyContacts`（取得自己新增過的好友）、`addMyContact`（新增一筆）、`deleteMyContact`（刪除一筆），都會依登入身分自動綁定「只存取自己的資料」，寫在 `js/cloud/cards-referrals-nfc.js`，並重新打包進 `cloud.js`。
  2. 「＋新增好友」表單送出後，改成呼叫上述雲端 API 存進 `directory_contacts`，不再只寫 localStorage；換裝置、換瀏覽器登入同一個帳號都看得到自己新增過的好友。
  3. 通訊錄列表、公司篩選清單（依「新增好友」的公司名稱自動累積）都改成讀雲端資料。
  4. 新增「刪除」按鈕：手動新增的好友資料列表現在可以直接刪除，不會像過去一樣新增了就永久留著、無法清除。
  5. **舊資料自動搬遷**：使用者原本留在瀏覽器 localStorage 裡的舊「新增好友」資料，登入通訊錄頁時會自動搬進雲端帳號底下（只搬一次），搬完才清掉本機備份，確保不會憑空消失。
  6. 「匯入聯絡人」（CSV 批次匯入）分頁目前仍是尚未接上真實匯入邏輯的展示功能，這次沒有變動，維持原狀。
- **影響範圍**：只改「平台通訊錄」頁面（`directory.html` 與 `js/pages/directory/`）與新增的雲端 API；不改公開 `card.html?id=...`、不改 `card.html?nfc=...`、不改「邀請好友」與「訪客免登入建立名片」流程。
- **快取更新**：`directory.html` 內 `cloud.js`／`directory-a.js`／`directory-b.js` 版本參數升為 `?v=20260812a`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.7`。

## 2026-08-12（重大修復：好友透過邀請連結無法建立自己的名片）

### 修正（Supabase Edge Function：guest-card-intake，好友免登入建立名片）

- **問題緣起**：使用者回報從「邀請好友」連結或「＋新增好友」進來的朋友，填完資料送出後無法完成註冊。
- **查出的真正原因**：
  1. 8 月初上線的「查重機制」（避免同一個人重複建立名片）裡有一個查詢語法錯誤：只填「電話」或只填「Email」其中一項時（這是最常見的填法），系統送出的過濾條件寫成 `phone.eq.xxx`，但資料庫要求的正確寫法必須是 `phone=eq.xxx`。
  2. 這個錯字會被資料庫直接當成「看不懂的參數」整個忽略，等於查詢完全沒有加任何篩選條件，變成「不管填什麼資料，都撈資料庫裡隨便一筆名片」。
  3. 結果就是幾乎每一位朋友只要沒有同時填「電話」和「Email」兩項，送出表單後都會被系統誤判成「已經註冊過」，直接被導向別人既有的名片，而不是建立屬於自己的新名片，推薦人數也不會增加。
  4. 這個 Edge Function 部署在 Supabase 雲端的版本，跟本機 repo 裡存的原始碼版本不一致（雲端已經是加了查重機制之後的版本，但這次的錯字從未同步回 git），已直接用 `supabase functions download` 把雲端實際運作中的程式碼抓回本機比對，才找到問題。
- **修正方式**：查重條件統一改用 `or=(...)` 的正確寫法，不論填一項或兩項聯絡方式都能正確比對，不會再誤判成重複。已重新部署到 Supabase（新版本代號 `2026-08-12-guest-card-intake-dedup-fix`），並實際測試「只填電話」「只填 Email」「重複送出同一支電話」三種情境皆正確。
- **資料復原說明**：診斷過程中發現，過去這個錯字造成「所有人都被導向」的其中一張名片（姓名：夏薇恩，建立於 2026-03-12），在清除測試資料時被誤刪，已立即用系統回傳的完整原始資料一字不差復原（建立時間、修改時間與所有欄位均與復原前相同），並確認網址可正常開啟。用於清理與復原的暫時性函式已在完成後立即從 Supabase 移除。
- **影響範圍**：只修正 `supabase/functions/guest-card-intake/index.ts` 的查重查詢邏輯；不改前端 `guest-join.html`、`settings.html` 邀請連結產生方式、不改資料表結構。

## 2026-08-05（改善：管理後台改用畫面內提示彈窗，取代瀏覽器原生 alert/confirm/prompt）

### 改善（admin.html → 操作提示與確認）

- **問題緣起**：全系統做「3C 小白友善度」檢視時發現，`admin.html` 大量使用瀏覽器原生 `alert()`（54 處）、`confirm()`（6 處）、`prompt()`（1 處），跟 `edit.html` 已經改用的白話文提示彈窗風格不一致，原生彈窗容易被漏看、無法統一樣式，日常操作（刪除名片、移除管理員、停用訂閱等）體驗比其他頁面明顯落後。
- **修正方式**：
  1. 新增 `showAdminFeedback()` 與對應的 `adminAlert()` / `adminConfirm()` / `adminPrompt()`，視覺沿用 `edit.html` save-feedback 的圓角卡片風格，行為與原生 `alert/confirm/prompt` 相同（一樣會擋住後續程式碼直到使用者按下按鈕），只是改成 Promise/await 寫法；若彈窗 DOM 意外不存在，會自動退回原生 `alert/confirm/prompt`，不會讓訊息整個消失。
  2. 全面把 `admin.html` 內的 `alert(...)` 換成 `await adminAlert(...)`、`confirm(...)` 換成 `await adminConfirm(...)`、唯一一處 `prompt(...)`（停用訂閱原因輸入）換成 `await adminPrompt(...)`；`copyLink()`、`openStickerModal()`、`openPdfExportModal()` 因為呼叫到彈窗，一併調整為可以正確等待彈窗結果的寫法。
  3. 逐一確認所有新增的 `await adminAlert/adminConfirm/adminPrompt` 呼叫都位在 `async function` 內，並用語法檢查確認整份檔案沒有壞掉。
- **影響範圍**：只改 `admin.html` 的提示/確認呈現方式，所有判斷邏輯、資料操作順序、API 呼叫完全不變；不影響 `card.html`、`edit.html`、`guest-join.html` 等其他頁面。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.11`。


## 2026-08-05（新增：常見問題頁與邀請好友加入頁 UX 加強）

### 新增（faq.html → 使用者自助說明）

- **問題緣起**：全系統做「3C 小白友善度」檢視時發現，除了 `edit.html` 內建的欄位說明外，整個系統沒有任何獨立的「怎麼用／常見問題」頁面；使用者卡住時只能直接聯絡管理員，重複問題無法自助解決。
- **新增內容**：
  1. 新增 `faq.html`，整理成四大類：收到別人分享的名片連結、被邀請建立自己的名片、登入卡住怎麼辦、名片本人（已登入使用者）操作，以及企業員工／NFC 相關問題，共約 12 題手風琴式收合問答，底部附 LINE 官方帳號聯絡入口。
  2. 在 `settings.html`「編輯名片」下方新增「常見問題」項目；`auth.html` 底部連結列新增「常見問題」；`guest-join.html` 表單下方新增「操作上有疑問？先看常見問題」連結。
  3. `service-worker.js` 的 `STATIC_ASSETS` 加入 `faq.html`，確保離線／PWA 情境下也能開啟。
- **影響範圍**：純新增頁面與導覽連結，不改任何既有登入、編輯、名片顯示邏輯。

### 改善（guest-join.html → 建立成功／查重命中畫面）

- **問題緣起**：上一版查重機制上線後，「已經有名片」的提示只是一行純文字，跟「新建立成功」的畫面幾乎長得一樣，3C 小白不容易一眼看出差異、不確定下一步該做什麼。
- **修正方式**：成功畫面新增圖示（✅ 新建立／📇 查到既有名片）與一行白話說明文字，明確告知「這不是新建立的名片」以及下一步要按哪個按鈕，不需要使用者自己讀懂差異。
- **影響範圍**：只改 `guest-join.html` 建立成功畫面的呈現方式，不改查重判斷邏輯、不改資料庫欄位。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.10`。


## 2026-08-05（修正：邀請好友加入頁不再直接顯示技術錯誤代碼）

### 修正（guest-join.html → 建立名片失敗提示）

- **問題緣起**：全系統做「3C 小白友善度」檢視時發現，`edit.html` 的存檔失敗提示已經刻意不把英文錯誤代碼直接顯示在畫面上（`save-feedback.js` 有明確註解「絕不直接拼進畫面文字」），但 `guest-join.html`（給完全不熟悉系統的訪客/朋友填的頁面）沒有跟上同一個原則，送出失敗時會直接顯示「錯誤代碼：CARD_CREATE_FAILED」這類英文技術代碼，對象反而是系統裡最不懂技術的使用者。
- **修正方式**：
  1. 新增 `friendlyGuestJoinErrorMessage()`，把 `guest-card-intake` 可能回傳的錯誤代碼（邀請已過期、系統忙碌、設定未完成等）轉成白話中文說明。
  2. 技術錯誤代碼改成只印到瀏覽器 console（`console.error`），畫面文字一律顯示白話說明，方便你之後直接對照 Supabase 後台 `guest-card-intake` 的 Logs 排查，不需要靠訪客回報一串代碼。
- **影響範圍**：只改 `guest-join.html` 送出失敗時的提示文字；不改建立名片的成功流程、不改查重邏輯、不改 `card.html?id=...` / `card.html?nfc=...` 網址規則。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.9`。


## 2026-08-05（修正：邀請好友／員工邀請避免同一人建立兩張互不相通的名片）

### 修正（guest-card-intake → 免登入建立名片）

- **問題緣起**：後台會看到同一個人出現兩筆名片資料。追查後確認 `guest-card-intake` 這個免登入建立名片的 Edge Function，每次送出都用 `crypto.randomUUID()` 直接建一筆新資料，完全沒有查重機制；只要同一個人被重複邀請、重複點連結，或表單被重新送出第二次，就會各自產生一張互不相通的公開名片。
- **修正方式**：
  1. 新增 `findExistingCardByContact()`：送出前先用電話或 Email（只在有值時才比對）查詢 `cards` 表是否已有相符資料。
  2. 若查到既有名片，不再新建，直接回傳既有名片的 `public_url` / `share_url`，並標記 `duplicate: true`；若這次送出是透過員工邀請連結，仍會把該邀請標記為已使用，避免同一組連結被無限重複領用。
  3. `guest-join.html` 收到 `duplicate: true` 時，成功畫面標題改顯示「這支電話／Email 已經有名片了，這是你的名片連結」，不再顯示「名片已建立完成」造成使用者誤會又建立了一張新名片。
- **影響範圍**：只改 `guest-card-intake` 送出前的查重判斷與 `guest-join.html` 對應提示文字；不改既有名片欄位、不改 `card.html?id=...` / `card.html?nfc=...` 網址規則、不影響原本沒有重複的正常建立流程。
- **待部署**：`supabase/functions/guest-card-intake/index.ts` 屬於 Supabase Edge Function，GitHub Pages 部署**不會**自動更新它，需要另外到 Supabase Dashboard 重新部署這個函式，新的查重邏輯才會生效。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.8`。


## 2026-08-05（修正：公開名片頁職稱空白時不再顯示「-」）

### 修正（公開名片頁 → 職務／職稱顯示）

- **問題緣起**：`card.html` 公開名片頁的職稱欄位（`viewerTitleZh` / `viewerTitleEn`）在未填寫時會 fallback 顯示一個「-」符號，訪客看到會誤以為是資料異常或殘缺，觀感不佳。
- **確認現況**：職稱欄位在 HTML 預設值與 JS 渲染（`titleZh || '-'` / `titleEn || '-'`）都會塞入「-」；姓名欄位（`viewerNameZh` / `viewerNameEn`）也有類似 fallback，但本次僅處理職稱，姓名維持原樣不變。
- **修正方式**：
  1. `viewerTitleZh` / `viewerTitleEn` 這兩個 DOM 節點初始改為 `display:none` 且無文字內容，避免載入瞬間閃過空字樣。
  2. JS 渲染邏輯改為：職稱有值才寫入文字並顯示該行；沒有值則清空文字並維持隱藏，不再 fallback 顯示「-」。
- **影響範圍**：只改公開名片頁職稱那一行的顯示邏輯；不影響姓名、公司、聯絡方式、公司驗證資訊區塊，不改 `card.html?id=...` / `card.html?nfc=...` 網址規則，不改資料庫欄位。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.7`。


## 2026-08-05（修正：公司驗證資訊改為依名片類型條件渲染，僅企業方案顯示）

### 修正（公開名片頁 → 公司驗證資訊）

- **問題緣起**：公開名片頁的「公司驗證資訊」區塊（trust panel）原本只要 `company`、統編、官方聯絡、最後更新任一有值就會顯示，與名片是否屬於已訂購企業方案的公司無關；個人名片只要填了公司欄位也會被誤判為需要顯示公司驗證資訊。
- **確認現況**（修改前先盤點既有元件與資料欄位，避免自行假設）：
  1. `card.html` 的 `trustPanel` 由 `renderTrustPanel(card, pj)` 填值並用 `.show` class 控制顯示，`card` 來自 `getCardPublic()`（`cards` 表 `select('*')`）。
  2. 盤點 `cards` 表既有欄位（`supabase-setup.sql`、`enterprise-phase1.sql` 及既有 migrations）與 `subscriptions` / `pricing_plans`（個人月費/季費/年費訂閱，不分個人版／企業版）、`admin_users.managed_company`、`company_settings`，確認目前資料庫**沒有**任何欄位代表「此名片屬於已訂購企業方案的公司」。
- **修正方式**：
  1. 新增 `supabase/migrations/20260805190000_cards_is_enterprise_flag.sql`：於 `cards` 表新增 `is_enterprise BOOLEAN NOT NULL DEFAULT false` 欄位（含索引與欄位註解），做為公司驗證資訊顯示與否的旗標，預設所有名片皆為個人名片。
  2. `card.html` 的 `renderTrustPanel()` 改為先檢查 `card.is_enterprise === true`；非企業方案名片會直接 `panel.remove()` 移除節點，不再只靠 CSS `.show` class 隱藏；企業方案名片維持原本欄位填值與顯示樣式不變。
- **待辦（尚未包含在本次修改）**：目前後台尚無 UI 可將 `is_enterprise` 設為 `true`，需先套用上述 migration，並由 super admin 手動於 Supabase 將對應公司名片的 `is_enterprise` 更新為 `true`，或後續再開發企業後台批次設定介面。
- **影響範圍**：只改 `card.html` 公司驗證資訊區塊的渲染條件與新增資料庫欄位；不改 `companyInfoSection`（公司資訊區塊）、不改聯絡方式、不改既有 `card.html?id=...` / `card.html?nfc=...` 網址規則、不影響現有已訂閱的個人訂閱系統。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.6`。

## 2026-08-05（客服統一改由曜鼎科技客服中心接收）

### 調整

- 設定頁、公開名片、編輯預覽與範例名片內的「產品客服／NFC 申請」連結，由舊帳號 `@632nedvu` 改為曜鼎科技客服中心 `@600kyyvp`。
- 開啟 LINE 時預先帶入「數位身分平台客服」，供單一客服後端自動加標籤與分派。
- 使用者自己的個人 LINE、官方 LINE 聯絡方式與已發出 NFC 公開網址完全不變；只調整平台客服連結。
- Service Worker 快取版本更新為 `v1.37.6`，避免手機繼續顯示舊客服帳號。

## 2026-08-05（檢視：新增聯絡方式符合現況與新手操作）

### 修正（編輯名片頁 → 新增聯絡方式）

- **檢視結果**：目前新增聯絡方式共有公司電話、個人電話、Email、網站、LINE、官方 LINE、Facebook、Instagram、LinkedIn、X、YouTube、微信、WhatsApp、其他。公開名片按鈕與資料格式大致可用，但部分平台的新增說明與輸入防呆不夠符合現況，3C 小白容易照著填錯。
- **修正重點**：
  1. 個人 LINE 改成明確要求貼「加好友連結」，不再讓使用者誤貼 LINE ID；說明同步加入 LINE 新版與舊版取得行動條碼連結路徑。
  2. 官方 LINE 可直接輸入 `@官方帳號ID`，系統會自動轉成 LINE 官方建議的 `line.me/R/ti/p/%40...` 連結。
  3. Facebook、LinkedIn、X、YouTube 的提示改成「找不到複製連結時可直接輸入公開帳號」，降低新手操作門檻。
  4. YouTube 修正只輸入 `@頻道帳號` 時會變成錯誤網址的問題，現在會轉成 `https://www.youtube.com/@...`。
  5. WhatsApp 支援台灣使用者直接輸入 `0912345678`，系統會自動轉成 `https://wa.me/886912345678`。
  6. 空白輸入提前阻擋，避免網站、YouTube 等類型被整理成無效的 `https://` 或 `https://@...`。
  7. 將 LINE、社群與 WhatsApp 的輸入整理規則集中到 `edit-contact-normalizers.js`，讓 `edit-chunk-3.js` 維持在 900 行內。
  8. 站內固定「LINE 官方帳號」連結改成 `%40` 編碼格式，與 LINE 官方帳號連結規格一致。
- **影響範圍**：只改編輯頁「新增聯絡方式」的提示文字、前端輸入整理與快取版本；不改公開 `card.html?id=...`、不改 `card.html?nfc=...`、不改 Supabase 專案、不改資料表。
- **快取更新**：`edit.html` 內 `edit-contact-normalizers.js` 版本參數升為 `?v=20260805b`，`edit-chunk-3.js` 版本參數升為 `?v=20260805c`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.5`。

## 2026-08-05（修正：Instagram 新增步驟符合現況）

### 修正（編輯名片頁 → 新增 Instagram）

- **問題緣起**：新增 Instagram 聯絡方式時，彈窗仍顯示舊版操作「右上角 ≡ → QR 圖碼 → 分享 → 複製連結」，與目前 Instagram App 的常見介面不一致。
- **修正方式**：
  1. Instagram 說明改為「右下角自己的頭像 → 分享個人檔案 → 複製連結」。
  2. 明確提示如果看不到「複製連結」，可直接輸入 `@帳號`，系統會自動整理成 Instagram 連結。
  3. Instagram 彈窗標題改為「輸入 IG 帳號或連結」，避免使用者誤以為一定要先找完整網址。
  4. 英文說明同步更新為 `Share profile` 與 `Copy link`。
- **影響範圍**：只修正編輯頁 Instagram 說明文字與快取版本，不改名片資料格式、不改公開 `card.html?id=...`、不改 `card.html?nfc=...`、不改 Supabase。
- **快取更新**：`edit.html` 內 `edit-chunk-3.js` 版本參數升為 `?v=20260805b`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.4`。

## 2026-08-05（維護：編輯頁拆分與快取更新）

### 維護（編輯名片頁）

- **問題緣起**：編輯頁部分 JavaScript 檔案接近或超過專案約定的 900 行，長期維護時不利於排查問題。
- **調整內容**：
  1. 新增 `js/pages/edit/edit-contact-normalizers.js`，集中處理微信號輸入整理。
  2. 新增 `js/pages/edit/edit-font-toolbar.js`，集中處理姓名、職稱與標語的文字樣式工具列。
  3. 新增 `js/pages/edit/edit-slogans.js`，集中處理標語新增與刪除。
  4. `edit-chunk-1.js`、`edit-chunk-2.js`、`edit-chunk-3.js` 都降到 900 行內，降低後續維護成本。
  5. `edit.html` 已加入新拆分檔案並更新版本參數；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.3`。
- **影響範圍**：只整理編輯頁前端檔案載入與快取，不改公開 `card.html?id=...`、不改 `card.html?nfc=...`、不改 Supabase 專案與資料表。

## 2026-08-04（新增：企業客戶追蹤待辦、匯出與一鍵聯絡）

### 新增（企業管理中心 → 客戶追蹤）

- **問題緣起**：企業管理員需要更像小型業務 CRM 的操作方式，不只看客戶留言，還要快速知道哪些客戶超過一天沒聯絡、能直接聯絡、能匯出名單交給業務跟進。
- **新增功能**：
  1. 客戶追蹤統計新增「超過 1 天未聯絡」，並以橘色提示優先處理名單。
  2. 客戶列表新增來源篩選（NFC、QR Code、LINE 分享、網頁）與時間篩選（超過 1 天未聯絡、今日新增、有聯絡方式）。
  3. 每筆客戶資料新增聯絡小標籤與操作按鈕，可直接撥電話、寄信、複製 LINE、複製完整客戶資料。
  4. 新增「匯出客戶」CSV，可把目前篩選後的客戶名單交給業務或主管追蹤。
  5. 新增「複製摘要」，可快速複製未聯絡、逾期未聯絡、已聯絡、成交、無效統計。
  6. 客戶狀態可從已聯絡 / 成交 / 無效改回未聯絡；改回未聯絡時會清除已聯絡時間，避免資料狀態混亂。
- **影響範圍**：只改企業後台客戶追蹤介面與既有更新函式，不新增資料表、不改 `lead_inquiries` 結構、不改公開名片與 NFC 連結。
- **快取更新**：所有引用 `cloud.js` 的頁面版本參數升為 `?v=20260804e`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.2`；已重新打包 `cloud.js` 與 `common.js`。

## 2026-08-04（改善：PWA 新版提示改為手動更新，不再突兀重整）

### 改善（手機 / PWA 快取更新）

- **問題緣起**：原本 Service Worker 偵測到新版本後，前端會直接執行 `window.location.reload()`，如果使用者正在編輯名片、看客戶資料或操作後台，可能會突然被重整打斷。
- **修正方式**：
  1. `service-worker.js` 的新版通知改為 `SW_UPDATE_AVAILABLE`，只告知頁面「有新版可用」。
  2. `js/common/sw-update.js` 改成顯示底部小提示，使用者可自行選擇「更新」或「稍後」。
  3. 只有使用者按下「更新」時才會重新整理頁面；按「稍後」會關閉提示，不影響目前操作。
- **影響範圍**：只改 PWA / Service Worker 更新提示，不改公開名片網址、不改 NFC 連結、不改 Supabase 資料表或 Edge Function。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.1`；引用 `common.js` 的頁面版本參數升為 `?v=20260804d`；已重新打包 `common.js`。

## 2026-08-04（部署：企業付費功能 Phase 1 後端正式上線）

### 部署（Supabase 正式專案）

- **部署內容**：
  1. 已將 migration `20260804163000_enterprise_paid_features_phase1.sql` 套用到正式 Supabase 專案 `nqxibryjhgftyxttopuo`。
  2. 已部署 `guest-card-intake` Edge Function，新版回應標記為 `x-uvaco-build: 2026-08-04-guest-card-intake-2`。
  3. 已確認遠端 migration 歷史包含 `20260804163000`，且再次 dry-run 顯示 `Remote database is up to date`。
- **正式啟用功能**：
  - 公開名片的「留下資料」可寫入 `lead_inquiries` 客戶待跟進資料表。
  - 企業後台「客戶追蹤」可讀取與更新待跟進狀態。
  - NFC 感應來源可寫入 `card_views.source = 'nfc'`，支援後台顯示最後 NFC 感應時間與 NFC 感應次數。
  - 企業邀請 token 流程已使用新版 `guest-card-intake`，不會把非登入訪客寫入 `card_invites.used_by`，避免外鍵錯誤。
- **影響範圍**：只套用已規劃的新增欄位、資料表、RLS policy、RPC 與 Edge Function；沒有改 `card.html?id=...`、沒有改 `card.html?nfc=...`、沒有刪除既有資料。
- **線上驗證**：
  - `company-admin.html` 回應 HTTP 200。
  - `card.html` 回應 HTTP 200。
  - `guest-card-intake` 線上端點已回應，狀態為 Supabase Edge Runtime。

## 2026-08-04（新增：企業付費功能 Phase 1，批次管理、客戶追蹤與防詐騙驗證）

### 新增（企業管理中心）

- **問題緣起**：使用者希望電子名片 V5 往企業願意付費的方向發展，重點包含員工批次管理、品牌模板鎖定、NFC 卡片狀態、客戶追蹤提醒，以及可讓客戶確認在職與公司官方資訊的防詐騙驗證頁。
- **新增功能**：
  1. `company-admin.html` 新增 3C 小白友善的企業管理流程提示：匯入員工、批次修改、輸出印刷資料。
  2. 員工列表新增勾選框與批次工具列，可批次改職稱、部門、分機、公司地址，也可批次停用 / 啟用。
  3. 新增「匯入員工名單」彈窗，可下載 CSV 範本、貼上或上傳名單、先預覽錯誤，再批次產生免登入員工建立連結。
  4. 單筆新增員工與批次匯入產生的邀請連結，改成 `guest-join.html?invite=...&openExternalBrowser=1`，新員工不需要 LINE、Google、簡訊或 Email 驗證碼。
  5. `guest-join.html` 可讀取企業邀請 token 並預填姓名、職稱、部門、公司、電話、Email、LINE，送出後仍由 `guest-card-intake` 後端自動審核通過。
  6. 公司設定的鎖定欄位擴充為姓名、公司 Logo、公司資訊與聯絡按鈕；員工編輯頁會套用對應鎖定效果。
  7. `card.html` 公開名片新增「公司驗證資訊」區塊，顯示公司、在職狀態、統編、官方聯絡方式與最後更新時間。
  8. `card.html` 新增「留下資料」表單；套用新版 Supabase migration 後，客戶送出資料會進入企業後台的待跟進名單。
  9. `company-admin.html` 新增「客戶追蹤」頁籤，可查看待跟進資料並標記未聯絡、已聯絡、成交、無效。
- **資料庫準備**：
  - 新增 migration `supabase/migrations/20260804163000_enterprise_paid_features_phase1.sql`。
  - 新增 `card_views.source`，用來分辨一般瀏覽、LINE 分享與 NFC 感應。
  - 更新 `get_card_view_summaries_for_admin()`，回傳 NFC 感應次數與最後 NFC 感應時間。
  - 新增 `lead_inquiries` 客戶待跟進資料表與 RLS 規則。
- **修復**：
  - 修正企業後台單筆編輯員工時，若沒有明確傳入 `profile_json`，可能把原本名片樣式、Logo、聯絡按鈕清空的風險；現在會保留既有 `profile_json`。
- **影響範圍**：不改 `card.html?id=...`、不改 `card.html?nfc=...`、不改既有 NFC 卡片 ID、不刪除任何資料表或欄位。客戶追蹤與最後 NFC 感應時間需等正式 Supabase 套用 migration 後才會完整啟用；未套用前頁面會顯示友善提示，不影響既有名片瀏覽。
- **快取更新**：所有引用 `cloud.js` 的頁面版本參數升為 `?v=20260804c`；`edit.html` 的 `edit-chunk-1.js` 版本參數升為 `?v=20260804c`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.37.0`。

## 2026-08-04（修復：新增微信後按鈕不可用）

### 修復（編輯名片 → 新增聯絡方式 → 微信）

- **問題緣起**：使用者在編輯名片新增「微信」後，輸入微信號雖然會出現在名片按鈕上，但程式把微信號原樣存成 `href`，例如 `href="wxid_..."`；這不是手機瀏覽器可開啟的網址，所以公開名片點擊時看起來像「無法使用」。
- **修正方式**：
  1. 編輯頁新增或編輯微信時，可直接輸入微信號（例如 `wxid_0endyjaii0ju12`），系統會轉成可嘗試開啟微信 App 的 `weixin://dl/chat?...` 連結。
  2. 公開名片頁點微信按鈕時，會先嘗試開啟微信；若手機或瀏覽器不支援外部開啟，會自動複製微信號並提示使用者到微信搜尋貼上。
  3. 加入通訊錄 `.vcf` 匯出時，微信號改放在備註，不再只塞一個手機通訊錄不一定認得的特殊網址。
  4. 載入舊名片時，如果舊資料裡已經有微信圖示但 `href` 只是微信號，編輯頁會在回填時自動修正成新版連結格式。
- **影響範圍**：只影響微信聯絡按鈕與 vCard 備註；電話、Email、LINE、WhatsApp、公開名片 `card.html?id=...`、NFC `card.html?nfc=...` 與 Supabase 資料表結構都不變。
- **快取更新**：`edit.html` 的 `edit-chunk-1/2/3/4.js` 版本參數升為 `?v=20260804b`；`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.36.9`。

## 2026-08-04（新增：好友免登入建立電子名片，系統自動審核通過）

### 新增（邀請好友 → 免登入填表 → 自動建立名片）

- **問題緣起**：使用者希望好友點「邀請好友」後，不要使用 LINE、Google、Email 驗證碼、手機簡訊或企業邀請碼；但又希望保留「管理員審查」概念，改成由系統自動審核通過。
- **新增功能**：
  1. 新增 `guest-join.html`，好友點邀請連結後可直接填姓名、職稱、公司、部門、電話、Email、LINE/聯絡連結，不需要登入。
  2. 新增 Supabase Edge Function `guest-card-intake`，前端送出後由後端檢查欄位、清除 HTML、限制長度，通過後用系統權限建立正式公開名片，並立即回傳 `card.html?id=<new_user_id>`。
  3. 「設定」頁既有邀請連結改成產生 `guest-join.html?ref=<user_id>&openExternalBrowser=1`；新的好友加入流程會自動記錄推薦人，舊的 `auth.html?ref=...` 登入邀請流程仍保留，不影響已分享出去的舊連結。
- **安全設計**：沒有開放匿名訪客直接寫入 `cards` 資料表；匿名訪客只能呼叫 `guest-card-intake`，由後端代為建立名片。這次也沒有做「私人編輯連結」，避免連結被轉傳後任何人都能修改名片；後續資料修改預設由管理員處理。
- **影響範圍**：只影響「之後新產生的邀請好友連結」；公開名片 `card.html?id=...`、NFC `card.html?nfc=...`、既有 LINE 登入使用者、管理員後台與 Supabase 既有資料表結構都不變。
- **快取更新**：重新打包 `cloud.js`，所有引用 `cloud.js` 的頁面版本參數升為 `?v=20260804a`；新增 `guest-join.html` 至 Service Worker 靜態快取清單，`CACHE_VERSION` 升級為 `v1.36.8`。

## 2026-08-04（修復：LINE 訊息分享名片時，優先改用外部瀏覽器開啟）

### 修復（公開名片頁 → LINE 訊息分享）

- **問題緣起**：名片網址如果是從 LINE 訊息點開，好友常會被 LINE 內建瀏覽器開啟；這會讓「加入通訊錄」或下載 `.vcf` 檔案的體驗不穩，使用者容易以為名片功能壞掉。
- **修正方式**：
  1. `card.html` 分享、複製連結、LINE 分享、Email 分享與 QR Code 顯示使用同一個分享網址，並自動補上 LINE 官方支援的一般網址參數 `openExternalBrowser=1`。從 LINE 訊息點開時，LINE App 會優先嘗試用外部瀏覽器開啟；其他瀏覽器會忽略這個參數，不影響名片讀取。
  2. 若好友仍在 LINE 內建瀏覽器中開啟公開名片，頁面底部會顯示清楚提示，提供「外部瀏覽器開啟」「複製連結」「繼續瀏覽」三個選項，避免使用者卡在 LINE 內建瀏覽器不知道下一步。
  3. 外部開啟按鈕會先嘗試使用 LIFF 的外部開啟能力；若目前不是 LIFF 環境，就改用一般網址導向，並保留複製連結備援。
- **影響範圍**：只修改公開名片頁的分享網址與提示列，不改 `card.html?id=...` / `card.html?nfc=...` 既有網址規則，不改 Supabase 專案、資料表或 NFC 綁定資料。未從 LINE 訊息開啟的使用者不會看到這個提示列。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.36.7`，讓手機可以取得新版 `card.html` 分享行為。這次沒有修改 `cloud.js` / `common.js`，不需要重新打包。

## 2026-08-03（修改：輸出貼紙PDF的 QR Code 改成真正的向量繪製，並加註向量格式說明）

### 修改（NFC 名片量產管理 → 輸出貼紙PDF）

- **問題緣起**：印刷廠商要求交付檔案必須是「ai、cdr、pdf 向量圖」三選一，因為貼紙裁切機需要向量路徑資料才能正確裁切，點陣圖（一般照片格式）放大會模糊、也無法拿來當裁切依據。確認後，網頁技術上沒辦法直接產生 `.ai`（Illustrator）或 `.cdr`（CorelDRAW）這兩種軟體公司的私有格式檔案（瀏覽器沒有能力寫出這類專屬格式，硬做只會生出打不開的假檔案，所以不會這麼做）；但使用者列出的三選一裡「PDF」本身就是合法選項，印刷廠商可以直接用 Illustrator 或 CorelDRAW 開啟/匯入 PDF、把裡面的向量內容當成可編輯物件使用。
- **原本的落差**：PDF 裡的姓名、職稱、公司名等文字，本來就是用 jsPDF 內嵌字型直接繪製，已經是向量文字；但 QR Code 原本是先在畫面外用 canvas 畫出來、截圖轉成 PNG 圖片再貼進 PDF，嚴格來說是點陣圖，不是 100% 向量。
- **這次的修改**：
  1. QR Code 改成直接讀取 QR 函式庫（qrcodejs）內部算好的黑白方格矩陣資料，逐格用 PDF 的向量色塊（矩形填色）畫出來，不再經過 canvas 截圖這一步。QR Code 本身就是規則的黑白方塊組合，這樣畫出來的結果跟原本肉眼看起來一樣，但放大到再大都是清晰的直角，不會模糊、也不會有點陣圖特有的鋸齒感。
  2. 產生的 PDF 最後一頁「貼紙材質/顏色 統計表」新增一段格式說明文字，明確告知印刷廠商「本檔案為 PDF 向量格式（文字與 QR Code 皆為向量繪製，非點陣圖），可直接交付印刷，或以 Illustrator／CorelDRAW 開啟匯入編輯」，避免廠商還要另外詢問檔案格式。
  - 個人 Logo 圖片維持點陣圖嵌入不變：廠商/使用者上傳的 Logo 多半是 JPG/PNG 這類點陣圖，技術上無法自動轉換成真正的向量圖（除非之後改成要求上傳 SVG 向量檔，這次不處理，不影響本次交付需求，因為 Logo 本來就不在裁切線範圍內）。
- **技術重點**：
  - `admin.html` 新增 `getQrModules()`（取代原本的 `qrCodeToDataUrl()`），直接讀取 `qrcodejs` 函式庫內部 `_oQRCode.getModuleCount()` / `_oQRCode.isDark(row, col)` 拿到 QR 矩陣資料，不再需要 canvas 截圖與 `setTimeout` 等待。
  - 新增 `drawVectorQr()`，用 `doc.rect(x, y, w, h, 'F')`（jsPDF 內建的向量填色矩形）把矩陣逐格畫成黑白色塊，取代原本的 `doc.addImage(qrDataUrl, 'PNG', ...)`。
  - `generateStickerPdf()` 呼叫方式同步調整；`addStickerStatsPage()` 加上格式說明文字段落，並讓下方材質/顏色統計表格的起始位置依說明文字實際換行行數自動往下挪，避免重疊。
  - 已確認 `qrCodeToDataUrl` 舊函式只有這一處呼叫（後台另一個「顯示個人 QR Code」的彈窗功能是完全獨立的程式碼，沒有共用這支函式），所以直接整支替換不會影響到其他功能。
- **實測驗證**：已下載並檢查目前引用的 `qrcodejs 1.0.0` 函式庫原始碼，確認 `getModuleCount()` / `isDark()` 這兩個內部方法確實存在且會同步算好資料，不是憑空猜測的寫法；程式碼修改後需要實際到後台勾選名片、輸出貼紙 PDF，放大檢查 QR Code 邊緣是否清晰、用手機掃描確認連結正常、確認統計頁說明文字沒有跟表格重疊，這部分請依下方驗證清單實際操作一次確認。
- **影響範圍**：只改變 QR Code 的繪製方式與統計頁新增一段文字，正反面版面座標、姓名/職稱/電話/Email/Logo/公司名稱等其他內容完全沒有變動；後台「顯示個人 QR Code」的既有彈窗功能不受影響。
- **快取更新**：`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.36.6`（這次只改了 `admin.html` 跟 `service-worker.js`，沒有動到 `cloud.js`，不需要重新打包）。

## 2026-07-23（新增：貼紙PDF輸出新增「材質/顏色」選擇與統計總表）

### 新增（NFC 名片量產管理 → 輸出貼紙PDF）

- **問題緣起**：量產名片貼紙時，同一批要出貨的名片，有些要用「PVC（可印製）」材質，有些要用「金屬材質（水晶貼）」，顏色也各自不同（PVC 分白/黑，金屬材質分黑/銀/黃），但原本「輸出貼紙PDF」功能完全沒有地方可以記錄、選擇這些資訊，只能口頭或另外用紙筆轉達給印刷廠商，容易出錯、對不起來。
- **新增功能**：
  1. 「NFC 名片量產管理」的名片列表新增「貼紙材質/顏色」欄位，每張名片可以點「設定」開啟卡片式選擇彈窗（5 種合法組合：PVC・白、PVC・黑、金屬(水晶貼)・黑、金屬(水晶貼)・銀、金屬(水晶貼)・黃/金），選好後會存進資料庫，之後隨時可以查詢、修改，不是只能在輸出當下臨時選一次。
  2. 工具列新增「🏷️ 批次設定材質/顏色」按鈕，沿用既有的勾選機制，一次把同一種材質/顏色套用到目前勾選的所有名片，同一批裡要跟別人不同的，再單筆改一次即可。
  3. 按「🖨️ 輸出貼紙PDF」時，若勾選的名片裡有尚未設定材質/顏色的，會顯示提醒「⚠️ 有 N 張尚未設定貼紙材質/顏色⋯」，但不會擋住輸出，統計表會把這些標示為「未設定」方便事後補登。
  4. 產生的 PDF 最後多加一頁 A4「貼紙材質/顏色 統計表」，依材質+顏色分組列出各自張數與輸出日期、總張數，直接附在同一份 PDF 裡就能給印刷廠商核對用量，不用另外準備 Excel。貼紙本身的正反面版面設計完全沒有變動。
- **技術重點**：
  - 新增 migration [supabase/migrations/20260723172342_add_sticker_material_color.sql](supabase/migrations/20260723172342_add_sticker_material_color.sql)，`cards` 資料表新增 `sticker_material`（`pvc`/`metal`）、`sticker_color`（`white`/`black`/`silver`/`gold`）兩個可為 NULL 的欄位，已用 Supabase CLI（本機已登入並連接到正式專案）`supabase db push` 套用到正式資料庫，舊資料不用回填。
  - `js/cloud/admin-operations.js` 新增 `setCardStickerOption()`，比照既有 `setNfcCardId()` 的管理員權限檢查模式；`js/cloud/index.js` 同步匯出給 `admin.html` 呼叫。
  - `admin.html` 新增 5 選項卡片式選擇彈窗（比照 `edit.html` 主題選擇彈窗的視覺風格，而非下拉選單）、單筆/批次兩種設定入口，以及 `generateStickerPdf()` 最後呼叫新增的 `addStickerStatsPage()` 產生統計頁。
- **實測驗證**：本機起了臨時測試伺服器，用瀏覽器模擬已登入的超級管理員身分（不影響任何正式資料）確認：名片列表正確顯示「PVC・白」「金屬(水晶貼)・銀」「未設定」等狀態；單筆設定彈窗點選卡片會正確用綠框標示選取、按儲存會呼叫設定函式並帶入正確的名片 ID；批次設定對 2 筆勾選名片同時套用成功；輸出貼紙PDF 彈窗在有未設定的名片時正確顯示提醒文字；直接驗證統計頁分組計算邏輯，確認「PVC・白 2 張、金屬(水晶貼)・銀 1 張、金屬(水晶貼)・黃/金 1 張、未設定 1 張」等分組與排序完全正確；過程中主控台沒有出現任何新增的錯誤。
- **影響範圍**：只新增功能，不影響既有「勾選→輸入英文公司名→產生正反面 PDF」的流程與 NFC 卡片 ID 綁定功能；舊資料因為兩個新欄位允許 NULL，現有名片都會自動顯示「未設定」，不需要額外處理。
- **快取更新**：重新打包 `cloud.js`，12 個引用 `cloud.js` 的頁面版本參數統一升級為 `?v=20260723a`；`service-worker.js` 的 `CACHE_VERSION` 同步升級為 `v1.36.5`。

## 2026-07-21（封存：Google 登入先暫緩，畫面恢復成只有 LINE 登入）

### 封存（Google 登入功能，非刪除）

- **問題緣起**：Google 登入這幾天陸續在做（前端串接、後端部署、帳號連結、主畫面捷徑相容性修復），但申請 Client ID、設定 Google Cloud Console、處理各種平台限制的過程對使用者來說太麻煩，決定先暫緩，畫面恢復成使用者原本熟悉、只有 LINE 登入的樣子。
- **做法**：把 `js/cloud/constants.js` 的 `GOOGLE_CLIENT_ID` 改回空字串——這是 Google 登入功能原本就設計好的「開關」，系統偵測到沒有 Client ID 就會自動隱藏所有 Google 相關按鈕（`auth.html` 的「使用 Google 登入」、`settings.html` 的「連結 Google 帳號」），**不需要刪除任何程式碼**。
- **這次「封存」而非「刪除」的原因**：所有 Google 相關的程式碼（前端按鈕邏輯、後端 Edge Function、帳號連結安全機制）跟 Supabase 後端設定（`google_identities` 資料表、已部署的 `google-auth` Edge Function、Secrets 金鑰）**全部保留、完全沒有動**——反正沒有 Client ID 就沒有任何按鈕會呼叫到這些東西，放著不影響任何人。以後如果想重新啟用，只要把 Client ID 貼回 `js/cloud/constants.js`（`155377373955-b609lj8sd79sebtaddsvo58g5t6p1mfk.apps.googleusercontent.com`）、重新打包 `cloud.js`、升版部署即可，**不需要重新申請 Google Cloud Console、不需要重新設定 Supabase、不需要重新寫任何程式碼**。
- **實測驗證**：本機瀏覽器確認 `auth.html` 只剩「用 LINE App 開啟」，Google 按鈕已消失、跟功能上線前一模一樣；LINE 登入流程（LIFF 自動登入、callback 判斷）運作正常，主控台沒有新增的錯誤。
- **影響範圍**：一般使用者看到的畫面回到只有 LINE 登入，跟這幾天做 Google 登入之前完全一樣；LINE 登入本身、其他所有功能不受任何影響。
- **快取更新**：重新打包 `cloud.js`，所有引用頁面版本參數升為 `?v=20260721c`；`service-worker.js` 的 `CACHE_VERSION` 同步升為 `v1.36.4`。

## 2026-07-21（修復：手機主畫面捷徑點「連結 Google 帳號」出現 Google 錯誤畫面）

### 修復（偵測「主畫面捷徑」模式，登入前先攔下並友善提示）

- **問題緣起**：使用者用手機「加入主畫面」的捷徑圖示開啟網站，點「連結 Google 帳號」後，Google 直接回應「已封鎖存取權：要求不符合 Google 政策」、「錯誤 403：disallowed_useragent」，整個流程卡住。
- **根本原因**：iOS 的「加入主畫面」捷徑（獨立網頁應用程式模式）點擊外部連結時，會用一種受限的內嵌瀏覽器模式開啟（畫面上方會出現「‹ Safari」的返回小字），這正是 Google 從 2017 年起就明確禁止的「內嵌瀏覽器」類型，基於帳號安全考量一律擋下 OAuth 登入請求。這是 Apple／Google 平台層級的限制，不是我們的程式碼寫錯，前端也沒辦法強制繞過或修好這個限制本身。
- **修正方式**：在觸發 Google 登入／連結流程之前，先偵測目前是不是「主畫面捷徑模式」（`navigator.standalone` 或 `display-mode: standalone`），是的話直接攔下、不送出登入請求，改成友善提示：「請切換到手機內建的瀏覽器 App（Safari/Chrome）貼上網址開啟後再試一次」，並自動把網址複製到剪貼簿方便貼上，避免使用者一路點到 Google 才看到一頭霧水的英文錯誤畫面。「使用 Google 登入」（`auth.html`）跟「連結 Google 帳號」（`settings.html`）兩個入口都加上這個防護。
- **附帶提醒**：這次順便發現 Google Cloud 專案裡的「品牌」設定（應用程式名稱）還是舊專案殘留的「poll」，導致 Google 同意畫面顯示的名稱不是「數位電子名片」——這個純粹是 Google Console 裡的顯示設定，跟程式碼無關，已請使用者自行到 Google Cloud Console →「品牌」把應用程式名稱改掉。
- **實測驗證**：本機瀏覽器用腳本模擬「主畫面捷徑模式」呼叫登入函式，確認正確被攔下、不會跳轉到 Google（`navigated:false`）；一般瀏覽器分頁測試維持正常，照常導向 Google 不受影響。因為需要在真實 iPhone 主畫面捷徑環境才能完整重現原本的錯誤畫面，實際手機測試需要使用者依下方驗證清單確認。
- **影響範圍**：只在偵測到主畫面捷徑模式時多一道攔截提示，一般瀏覽器分頁（包含桌機、手機 Safari/Chrome 分頁）完全不受影響。
- **快取更新**：重新打包 `cloud.js`，所有引用頁面版本參數升為 `?v=20260721b`；`service-worker.js` 的 `CACHE_VERSION` 同步升為 `v1.36.3`。

## 2026-07-21（新增：設定頁「連結 Google 帳號」，避免以後移除 LINE 登入時資料遺失）

### 新增（帳號連結功能）

- **問題緣起**：Google 登入昨天已經上線，但目前 Google 跟 LINE 是兩張完全獨立的身分對照表，互不關聯。如果同一個人先用 LINE 登入建了名片，之後改用 Google 登入，系統會查不到對照關係、直接生一個全新的空白帳號，看不到原本 LINE 帳號的名片資料。因為之後的目標是要把 LINE 登入整個移除，如果不先處理這個問題，移除的那一刻所有使用者都會「找不到」自己的帳號。
- **新增功能**：「設定」頁新增「🔗 連結 Google 帳號」項目，讓已經登入的使用者（不管目前用 LINE 還是 Google）可以主動把自己的 Google 帳號綁定到目前這個帳號，之後不管用哪種方式登入，看到的都是同一份名片資料。已連結會顯示「✅ 已連結 Google 帳號（信箱）」。
- **安全設計**：連結必須同時滿足兩個條件才會成功——① 呼叫者要先證明自己是目前合法登入中的使用者（後端會用簽發登入 JWT 用的同一把密鑰重新驗證簽章與有效期限，不相信前端自稱的任何身分）；② Google 帳號的所有權要經過 Google 官方同意畫面證明（沿用既有的 OAuth 換證流程），不是只憑使用者輸入的信箱就相信。另外也擋掉「同一個 Google 帳號被綁到兩個不同帳號」的情況：如果這個 Google 帳號已經連結過別人的帳號，會直接拒絕並提示「請換一個尚未使用過的 Google 帳號」。
- **技術重點**：
  - `supabase/functions/google-auth/index.ts` 新增 `action: "link"`／`action: "link_status"` 兩個分支，並新增 `verifyJwtHS256()`（跟既有簽發 JWT 用的 `signJwtHS256()` 互為驗證/簽發的一組）。原本沒帶 `action` 欄位的一般登入流程完全沒有更動，維持向下相容。
  - `js/cloud/oauth-google-apple.js` 新增 `startGoogleLink()`／`getGoogleLinkStatus()`，並讓既有的 `finishGoogleLoginFromUrl()` 多判斷一種 `google_link_` 開頭的 state（沿用 `google_` 前綴，確保跟既有的 LINE/Google callback 判斷順序不衝突），連結模式完成後**不會**覆蓋掉使用者目前的登入狀態，只會把結果透過網址參數帶回設定頁。
  - 連結流程重複使用跟一般 Google 登入相同、已經在 Google Cloud Console 登記過的 redirect_uri，使用者不需要再多做任何 Google Console 設定。
- **刻意不做的事**：沒有做「解除連結」功能（目前沒有這個需求，且涉及「萬一是唯一登入方式怎麼辦」的額外邊界情況，之後有需要再處理）；沒有做「登入時依信箱自動比對合併帳號」（比較不安全，容易被冒用信箱搶走別人帳號，所以採用需要使用者主動操作、且雙重驗證身分的連結按鈕）。
- **實測驗證**：部署後端後用 `curl` 直接測試邊界情況，確認：帶假造/過期的登入憑證會被正確拒絕（401）、完全不帶 `action` 欄位的一般登入請求行為跟原本完全一樣（向下相容）；本機瀏覽器開設定頁確認沒有登入狀態下「連結 Google 帳號」項目正確維持隱藏、頁面沒有新增任何主控台錯誤。因為需要一個已經登入的真實帳號才能完整測試「連結成功、換帳號登入看到同一份名片」這個核心情境，這部分請依下方驗證清單在正式網站實際操作一次確認。
- **影響範圍**：只新增功能，不影響現有 LINE／Google 登入流程；沒有連結過的使用者完全不受影響，用起來跟現在一樣。
- **快取更新**：重新打包 `cloud.js`，所有引用頁面版本參數升為 `?v=20260721a`；`settings.html` 內 `js/pages/settings/main.js` 版本參數升為 `20260721a`；`service-worker.js` 的 `CACHE_VERSION` 同步升為 `v1.36.2`。

## 2026-07-20（準備中：新增 Google 登入，之後要拿掉整個 LINE 登入流程）

### 新增（Google 登入，暫時跟 LINE 並存）

- **問題緣起**：使用者反映 LINE 登入的流程很擾人，想整個拿掉。但目前系統唯一能用的登入方式就是 LINE（Email 登入已停用、Google/Apple 登入程式碼寫好了但完全沒接上畫面按鈕、也沒設定金鑰），如果直接拿掉 LINE，會變成所有人都無法登入。
- **這次先做的事**：把「Google 登入」接上畫面，讓它可以跟 LINE 並存運作，等實測確認 Google 登入沒問題後，下一步才會把 LINE 登入整個移除。
  1. 新增 [google-login-setup.sql](google-login-setup.sql)：建立 `google_identities` 身份對照表（跟現有 LINE 登入用的 `line_identities` 是一樣的做法），**需要手動到 Supabase Dashboard 貼上執行一次**。
  2. `auth.html` 新增「使用 Google 登入」按鈕——這顆按鈕預設是隱藏的，只有偵測到 Google Client ID 已經設定完成才會自動顯示，避免使用者看到按了沒反應的按鈕。
  3. `js/pages/auth/main.js` 接上 `startGoogle()` 觸發登入，以及 `finishGoogleLoginFromUrl()` 處理 Google 登入完成後的回呼（且刻意排在 LINE 回呼判斷「之前」處理，避免 Google 導回來的網址被 LINE 那段比較寬鬆的判斷邏輯誤攔截）。
- **使用者接下來需要自己完成的設定**（無法透過程式碼自動完成）：
  1. 到 Google Cloud Console 申請一組 OAuth 2.0 用戶端 ID。
  2. 把 Client ID 填入 `js/cloud/constants.js` 的 `GOOGLE_CLIENT_ID`（我會協助填寫，只需要提供這組 ID）。
  3. 到 Supabase Dashboard → Edge Functions → Secrets，設定 `GOOGLE_CLIENT_ID` 和 `GOOGLE_CLIENT_SECRET`。
- **影響範圍**：目前 LINE 登入完全沒受影響，一般使用者看到的畫面也沒有變化（Google 按鈕還是隱藏的）；只有設定完成、且我幫忙把 Client ID 填進程式碼之後，Google 登入按鈕才會出現。**下一步（等 Google 登入實測沒問題後）才會正式把 LINE 登入按鈕跟相關流程整個移除**。
- **快取更新**：`auth.html` 內 `js/pages/auth/main.js` 版本參數升為 `20260720a`。這批修改涉及 Service Worker 會快取的檔案（`auth.html`），部署時記得依照上一則紀錄的提醒，同步把 `service-worker.js` 的 `CACHE_VERSION` 再往上加一格。

### 後端已部署完成（資料表＋Edge Function），只剩 Client ID／Secrets 待設定

- 用 Supabase CLI（本機已登入並連接到正式專案）完成以下兩件事，使用者不用再自己跑一趟 Dashboard 貼 SQL：
  1. 新增 migration [supabase/migrations/20260720143619_google_identities.sql](supabase/migrations/20260720143619_google_identities.sql) 建立 `google_identities` 對照表，已 `supabase db push` 套用到正式資料庫。
  2. 加碼資安加固：新增 migration [supabase/migrations/20260720143757_google_identities_rls.sql](supabase/migrations/20260720143757_google_identities_rls.sql)，把這張表開啟 RLS 且不建立任何政策，一律拒絕 `anon`／`authenticated` 直接存取；`service_role`（Edge Function 使用）依 Postgres/Supabase 慣例永遠會略過 RLS，既有存取行為完全不受影響。同步更新 `google-login-setup.sql` 裡的建表語法，讓之後如果改用手動貼到 Dashboard 執行，也會套用同一道防護。
  3. `supabase functions deploy google-auth --no-verify-jwt`：部署 Edge Function（因為這支函式的用途就是給「還沒登入」的使用者換發 JWT，所以跟 `line-auth` 一樣不能要求既有 JWT）。呼叫診斷端點（GET）確認已正常運作，且如預期回報 `google_client_id`／`google_client_secret` 尚未設定，不影響任何現有功能。
- **使用者只剩下這兩步**：① 到 Google Cloud Console 申請 OAuth 2.0 用戶端 ID（拿到後把 Client ID 給我，我會直接幫忙填入 `js/cloud/constants.js`；Client Secret 不要給我，直接在下一步設定）。② 到 Supabase Dashboard → Edge Functions → Secrets，設定 `GOOGLE_CLIENT_ID` 和 `GOOGLE_CLIENT_SECRET`。這兩步都需要在瀏覽器操作、輸入只有本人知道的帳密／金鑰，無法由 AI 代勞。

### Client ID／Secrets 設定完成，Google 登入按鈕正式上線（本機測試已通過）

- 使用者在 Google Cloud Console 申請好 OAuth 2.0 用戶端 ID 後，完成收尾：
  1. `js/cloud/constants.js` 填入正式的 `GOOGLE_CLIENT_ID`。
  2. **重新打包 `cloud.js`**（`npm run build:cloud`）——這一步容易被忽略但很關鍵：`constants.js` 是原始碼，實際載入到網頁上的是打包過的 `cloud.js`，只改原始碼不重新打包，網站看到的還是舊的空白 Client ID。
  3. 12 個有引用 `cloud.js` 的 HTML 頁面版本參數統一升級為 `?v=20260720b`，`service-worker.js` 的 `CACHE_VERSION` 同步升級為 `v1.36.1`（照上一則紀錄的提醒，改了 Service Worker 會快取的檔案就要記得同步升版，這次沒有再忘記）。
  4. 用 Supabase CLI 執行 `supabase secrets set` 設定 `GOOGLE_CLIENT_ID`／`GOOGLE_CLIENT_SECRET`。Client Secret 全程只在這一行指令用過，不會寫進任何程式碼或存進 git。
- **實測驗證**：
  - 呼叫 `google-auth` Edge Function 診斷端點，確認 `google_client_id`／`google_client_secret` 已回報為已設定。
  - 本機起了臨時測試伺服器，用瀏覽器打開 `auth.html`：確認「使用 Google 登入」按鈕正確顯示、主控台沒有新增的錯誤；實際點擊按鈕，確認正確帶著正式 Client ID 導向 Google 登入頁面（因為是本機網址不是正式網域，Google 那邊擋下 `redirect_uri_mismatch`，這是預期中、且證明安全設定正確的結果——換成正式網域網址就會放行）。
- **目前狀態**：程式碼與雲端設定都已完成，但**還沒推送（`git push`）到 GitHub**，所以正式網站目前還是舊版本，Google 登入按鈕還看不到。等推送上去、且使用者用真實 Google 帳號實測完整登入流程沒問題後，才會進入下一階段：正式移除 LINE 登入。

## 2026-07-20（修復：正式網站一直卡在很舊的版本，管理員上傳圖片還是顯示「不支援」的舊訊息）

### 修復（Service Worker 快取版本號沒有更新）

- **問題緣起**：管理員在正式網站點擊 Logo 準備上傳圖片，卻跳出「管理員模式目前不支援替他人上傳圖片，請由本人登入後更新頭像/Logo」的舊版警告訊息——但這段擋板程式碼其實已經在今天的改版裡拿掉了，照理說不應該再出現。
- **根本原因**：這個網站是 PWA（有安裝 Service Worker），`edit.html`、`css/edit-*`、`js/cloud|common|pages/*` 這些檔案都被設定成「快取優先」策略——瀏覽器會先直接用手機/電腦裡存的舊版本，之後才在背景偷偷更新快取，下次才看得到新版。這個機制本身需要開發者在 `service-worker.js` 手動把 `CACHE_VERSION` 版本號往上加一格，瀏覽器才會知道「有新版本了，把舊快取整個清掉重抓」；如果沒有動這個版本號，不管網站程式碼本身改了多少次、`edit.html` 裡的 `?v=...` 版本參數怎麼升級，Service Worker 都不會發現，會一直用它自己快取住的舊版本回應，導致今天做的所有修改（圖片裁切、大字模式、精靈引導、字體工具列定位…）在已經安裝過這個網站的裝置上完全看不到，只會看到很久以前的舊畫面。
- **修正方式**：把 `service-worker.js` 的 `CACHE_VERSION` 從 `v1.35.3` 升級為 `v1.36.0`。網站本身已經有寫好「偵測到新版本就自動重新整理」的機制，使用者不需要手動清快取，通常重新整理一兩次網頁（或重新開啟 App）就會自動套用新版本。
- **重要提醒（給未來的自己/協作者）**：以後只要有修改到 `edit.html`、`css/edit-*.css`、`js/cloud/**`、`js/common/**`、`js/pages/**` 這些會被 Service Worker 快取的檔案，**除了幫個別檔案的 `?v=...` 參數升級版本號之外，也一定要記得同步把 `service-worker.js` 的 `CACHE_VERSION` 往上加一格**，否則正式網站會一直卡在舊版本，之前做的修正即使程式碼已經正確、也已經部署，使用者實際打開網站時還是會看到舊的行為。
- **影響範圍**：只修改 `service-worker.js` 的版本號，不影響任何功能邏輯本身；這個修正本身部署後，才能讓今天稍早做的所有「編輯名片」相關修正（圖片裁切、大字模式、新手引導、字體工具列定位）真正在使用者的裝置上生效。

## 2026-07-20（修復：編輯姓名/職稱/標語時，字體樣式工具列會蓋住其他欄位）

### 修復（字體樣式工具列定位邏輯）

- **問題緣起**：手機上點擊姓名/職稱/標語欄位時，會跳出一個「字級/粗細/顏色」的字體樣式工具列，但這個工具列原本是用「固定往上飄 50px」的寫法定位，完全沒考慮頁面頂部固定工具列的實際高度、也沒考慮欄位彼此距離太近的情況，導致工具列常常疊到旁邊正在編輯的姓名或標語文字，把使用者正在看的內容擋住。
- **修正方式**：改成「智慧定位」：
  1. 量測工具列自己實際的寬高，不再假設固定是 50px。
  2. 量測頁面頂部固定工具列（返回列＋標題列，含主題/儲存等按鈕）目前實際佔用的高度——這個高度會因為大字模式、按鈕換行而變動，不能寫死。
  3. 額外檢查姓名/職稱/標語彼此之間的距離，如果正上方緊貼著另一個欄位（例如姓名跟第一個標語中間只有 30 幾像素），也會把那個欄位當作邊界。
  4. 上方空間足夠就浮在欄位正上方（維持原本習慣的操作方式）；空間不夠就自動改浮在欄位下方，絕對不會疊到其他欄位或頁面頂部工具列。
  5. 水平位置也會夾在螢幕寬度內，避免手機窄螢幕上工具列超出邊界被裁掉。
- **實測驗證**：本機以手機視窗尺寸＋大字模式重現問題情境（姓名後緊接著一個標語），確認修正後工具列無論是編輯姓名還是編輯標語，都不會疊到姓名、職稱、標語或頁面頂部工具列的任何一個，且完全在螢幕範圍內。
- **影響範圍**：只影響「編輯名片」頁面姓名/職稱/標語欄位的字體樣式工具列定位，不影響其他功能。
- **快取更新**：`edit.html` 內 `edit-chunk-1.js` 版本參數升為 `20260720b`。

### 二次修正（窄螢幕上工具列仍會貼齊邊緣、不好點）

- **問題緣起**：上面那版修正後，實測發現在較窄的手機螢幕上，工具列（字級/粗細/顏色那排按鈕）本身的寬度太接近甚至超過螢幕寬度，導致置中計算失準，整條工具列貼死在螢幕左邊緣，按鈕擠在一起不好準確點擊。
- **根本原因**：兩個問題疊加造成——① 工具列 CSS 原本沒有限制最大寬度、也不能換行，在窄螢幕上所有按鈕硬擠成一整排，寬度隨時可能超出螢幕；② 量測工具列尺寸的時機點量到的數字不穩定（同一顆工具列，量測時機不同，量到的寬度可能差了 80 幾像素），導致置中計算用了不準確的寬度，算出來的位置就跟著跑掉。
- **修正方式**：
  1. 工具列 CSS 加上 `max-width` 限制與允許換行成兩排，確保寬度不會超出螢幕。
  2. 定位邏輯改成「套用位置後再量一次實際尺寸校正」，不管一開始量到的尺寸準不準，套用後都會再確認一次工具列真正的寬高並修正位置，確保最終結果一定跟工具列實際呈現的樣子吻合。
- **實測驗證**：用 375px 寬的手機視窗重現問題，確認修正後工具列左右兩側都有留白、不貼邊，且不論編輯姓名或標語都不會疊到其他欄位。
- **快取更新**：`edit.html` 內 `edit-chunk-1.js` 版本參數再升為 `20260720c`，`edit-head.css` 版本參數升為 `20260720b`。

## 2026-07-20（新增：上傳頭像／Logo 後可以自己拖曳調整位置與大小）

### 新增（圖片裁切/縮放視窗）

- **問題緣起**：原本上傳頭像或 Logo 後，程式會直接把整張圖自動置中壓縮存檔，使用者完全沒辦法自己選要保留照片裡的哪個部分——例如大頭貼常常把想要的臉部裁掉一半，Logo 也可能被壓得走樣。
- **新增功能**：上傳圖片後，會先跳出一個裁切視窗，可以：
  - 用手指/滑鼠拖曳圖片調整位置。
  - 用下方滑桿放大縮小（1x～3x）。
  - Logo 額外提供「原始比例／正方形／橫式 2:1／橫式 3:1」四種裁切比例可切換；頭像固定 1:1（會顯示圓形裁切窗口，對應名片上最終圓形頭像的呈現方式）。
  - 選好範圍後按「完成」才會真的存成圖片，按「取消」則畫面維持原樣不受影響。
- **套用範圍**：編輯頁主畫面的「編輯 LOGO」「編輯頭像」，以及新手精靈裡「上傳頭像照片」步驟，兩邊共用同一套裁切邏輯，體驗一致。
- **技術重點**：全部自己寫（canvas + 拖曳/縮放數學），沒有引入任何第三方裁切函式庫，避免多一個外部套件的載入/相容性風險。裁切完的圖片還是會經過原本既有的自動壓縮流程（限制在 1MB 內），只是壓縮前多了「使用者自己選範圍」這一步。精靈的頭像上傳原本是直接把整張圖存成一長串文字（base64）塞進資料庫，這次順便修正成跟主畫面一樣，改成真正上傳到雲端 Storage，資料庫存的只是一個路徑，更有效率。
- **實測驗證**：本機瀏覽器實測，用四色象限測試圖驗證裁切數學完全正確（例如切到左上角時輸出圖片確實只包含左上角顏色，不含其他象限，像素採樣逐一比對過），確認 Logo 四種比例、頭像圓形裁切、拖曳＋縮放組合都運作正常，過程中主控台沒有出現新增的錯誤。
- **影響範圍**：只影響「編輯名片」頁面上傳圖片的流程，不影響其他頁面；已經上傳過的舊圖片不受影響。
- **快取更新**：新增 `css/image-crop.css`、`js/pages/edit/image-crop.js`；`edit.html` 內 `edit-chunk-2.js`、`wizard.js` 版本參數升為 `20260720b`。

## 2026-07-20（新增：「編輯名片」新手/長輩友善大改版）

### 新增與修復（讓不熟悉手機/電腦的長輩也能輕鬆編輯名片）

- **問題緣起**：很多使用者（尤其長輩）反映編輯名片頁面不知從何下手：畫面上同時有三套互相打架的新手教學、姓名職稱要直接點文字才能改（手機上完全看不出來）、刪除按鈕要滑鼠移過去才會出現（手機沒有滑鼠，等於看不到）、存檔失敗時直接跳出一整段英文技術錯誤訊息、字太小長輩看不清楚。這次做了一次整體檢查與改版。
- **意外發現並修好的既有 bug（使用者完全不會察覺，但功能其實已經壞掉）**：
  1. 新手精靈「選擇主題」步驟完成後，呼叫的是一個整個專案裡根本不存在的函式 `setCardTheme()`，因為外層包了防呆判斷，會靜默失敗——**精靈選的主題其實從來沒有真正套用到名片上**。已改成呼叫正確存在的 `applyCardThemeVisual()`。
  2. 精靈的「選擇主題」畫面自己維護了一份完全不同的 9 個假主題（顏色、名稱都跟正式選單對不起來），跟正式的 14 個主題編號、名稱、配色完全兜不上。已改成兩邊共用同一份 `window.UVACO_THEME_LIST` 資料（[edit-chunk-4.js](js/pages/edit/edit-chunk-4.js)）。
- **整合三套互相覆蓋的新手教學，變成清楚的兩個入口**：原本首次教學彈窗、一套已經失效的舊版浮動提示（[edit-chunk-5.js](js/pages/edit/edit-chunk-5.js) 死碼，因為後載入的 `wizard.js` 用同名函式蓋掉，永遠不會執行）、跟 5 步驟精靈三套並存互相打架。現在首次歡迎彈窗改成清楚的兩個選項：「🧙 用精靈逐步引導我（新手推薦）」或「✍️ 我自己直接編輯」（選這個會聚焦到姓名欄位，並顯示一張不會自動消失、要按「知道了」才關閉的提示卡，取代舊版 5 秒就消失、很容易被錯過的浮動提示）。
- **精靈擴充成「新增類操作」的統一入口，跟直接編輯並存**：
  - 頂部工具列新增常駐的「🧙 逐步設定」按鈕，任何時候都能重新叫出精靈；如果名片已經有資料，精靈會先帶入姓名/職稱/頭像/聯絡方式（新增 `prefillWizardFromCurrentCard()`），不會被清空重填。
  - 聯絡方式區塊新增「🧙 用引導方式新增」按鈕，點了會直接跳到精靈的「聯絡方式」步驟（單步驟模式：不顯示 5 步驟進度條、只有「新增這一項」一顆按鈕），跟旁邊原本的「+新增聯絡方式」並存，不互相取代——想直接點文字改既有內容，還是用精靈一步步新增，使用者自己選。
  - 若目前完全沒有任何聯絡方式，「用引導方式新增」按鈕會有輕微的呼吸動畫提示新手可以從這裡開始（`evaluateGuideEntry()`），已經有資料就不會搶眼。
- **儲存成功/失敗訊息全面白話文化**：`saveCard()` 原本共 9 處直接用瀏覽器原生 `alert()`/`confirm()`，失敗時甚至會把 `Bucket not found`、`RLS policy denied` 這種英文技術錯誤直接顯示給使用者看。全部改成自訂的白話文提示彈窗（新增 [save-feedback.js](js/pages/edit/save-feedback.js)），例如「網路好像不太穩定，請檢查一下 Wi-Fi 或行動網路，恢復後再按一次『儲存』」，技術細節只留在 console 除錯用，並提供「重新儲存」「複製錯誤代碼給客服」等對應動作按鈕。
- **手機上刪除按鈕看得到、按得到**：聯絡方式的刪除按鈕（`.contact-delete-btn`）原本要滑鼠移過去才會顯示，手機/平板完全看不到怎麼刪除。改成只有「真的用滑鼠」的桌機才維持原本 hover 才顯示的行為，觸控裝置一律常駐顯示。
- **新增「大字模式」開關，照顧長輩/視力不佳的使用者**：頂部工具列新增「一般／大字」切換（窄螢幕自動收成單一 🔠 圖示按鈕），開啟後會放大按鈕、標籤、精靈、刪除鈕、彈窗文字等「頁面控制項」，並記住狀態（不影響其他使用者的預設畫面）。刻意不放大名片預覽區裡姓名/職稱/標語等「代表最終名片外觀」的實際文字，避免長輩誤以為名片本身就會長這麼大字。
- **實測驗證**：本機以繞過登入的方式完整跑過一次瀏覽器實測（Chrome DevTools），確認：精靈選主題 10 後名片真的變成該主題顏色、精靈主題選單顯示的 14 個主題名稱與正式主題選擇器逐字一致、聯絡方式引導入口能正確新增一筆電話並顯示白話文成功提示、手機視窗（375px＋觸控模擬）下刪除按鈕不需 hover 就可見、桌面滑鼠視窗下維持原本 hover 行為、大字模式切換後控制項放大但名片文字大小不變且能正確記住狀態、「自己編輯」提示卡與呼吸動畫提示皆正常運作，過程中主控台沒有出現任何新增的錯誤。
- **影響範圍**：只影響「編輯名片」頁面（`edit.html` 與其對應的 `js/pages/edit/*`、`css/edit-*`），不影響其他頁面；一般（非長輩）使用者的預設畫面與操作流程完全不變，只是多了「大字模式」跟「精靈引導」這兩個可選的輔助入口。
- **快取更新**：`edit.html` 內 `edit-head.css`、`edit-wizard.css`、`edit-chunk-4.js`、`wizard.js` 版本參數升為 `20260720a`；新增 `css/edit-guide.css`、`css/edit-a11y.css`、`js/pages/edit/edit-a11y.js`、`js/pages/edit/save-feedback.js` 四個檔案。

## 2026-07-20（修復：「編輯名片」職稱欄位清空後，會自動跑出洗不掉的英文字「Title」）

### 修復（職稱欄位資料汙染）

- **問題緣起**：管理員在後台編輯某位使用者的名片時，發現名片上第二行（中文職稱的位置）一直卡著一個英文單字「Title」的灰色文字，怎麼點、怎麼刪都刪不掉，存檔重新整理後又跑回來。
- **根本原因**：找到 [edit-chunk-5.js](js/pages/edit/edit-chunk-5.js) 儲存名片的程式碼裡，有一行「如果中文職稱欄位是空的，就存英文字『Title』進資料庫」的舊邏輯。這個「Title」原本應該只是給工程師看的暫時代稱，結果卻被真的存進資料庫的職稱欄位裡，變成使用者洗不掉的髒資料；下次打開編輯頁時，[edit-chunk-1.js](js/pages/edit/edit-chunk-1.js) 讀資料的邏輯又會把這個髒資料撈出來顯示，兩邊加在一起就變成「刪了又跑回來」的無限迴圈。
- **修正方式**：
  1. 儲存邏輯改成：中文職稱空白時，優先退而求其次用英文職稱；兩者都空白，就老實存空字串，不再塞假的「Title」字樣進資料庫。
  2. 讀取邏輯加一道防呆：如果資料庫裡撈到的舊職稱剛好等於「Title」這個已知的髒值，一律當作沒有資料處理，讓已經中毒的舊名片重新整理後能自動復原成空白，可以正常編輯、正常刪除。
- **實測驗證**：程式碼邏輯已確認正確，因需要登入真實帳號才能完整重現「清空職稱→儲存→重新整理」的完整流程，建議依下方驗證清單實際操作一次確認。
- **影響範圍**：只影響「編輯名片」頁面的職稱欄位讀取／儲存邏輯，不影響姓名、標語、公司資訊等其他欄位，也不需要修改資料庫結構。已經中毒顯示「Title」的舊名片，只要重新打開編輯頁就會自動復原，不需要手動清資料庫。
- **快取更新**：`edit.html` 的 `edit-chunk-1.js` 版本參數升為 `20260720a`，`edit-chunk-5.js` 版本參數升為 `20260720a`。

## 2026-07-17（新增：管理員可以幫別人上傳大頭貼／Logo）

### 新增（管理員模式圖片上傳權限）

- **問題緣起**：`admin.html`「編輯」功能（`edit.html?adminMode=true`）原本可以幫別人改姓名/電話/Email 等文字欄位，但一換照片就會跳出「管理員模式目前不支援替他人上傳圖片」的提示、整筆修改都存不進去，必須請本人自己登入才能換照片，很不方便。
- **根本原因**：不只是前端擋住而已，Supabase 的 Storage 安全規則（RLS policy）原本寫死「只能把檔案傳到自己的資料夾」，就算拿掉前端的提示，後端還是會直接拒絕寫入。
- **修正方式**：
  1. 新增一份 SQL（[admin-upload-storage-rls.sql](admin-upload-storage-rls.sql)），在 Storage 安全規則加一條「管理員例外」，規則跟現有「管理員改文字欄位」完全一致：超級管理員什麼人的圖片都能傳，企業管理員只能傳自己公司員工的圖片。**這份 SQL 需要手動到 Supabase Dashboard → SQL Editor 貼上執行一次**，程式碼本身不會自動幫你跑資料庫。
  2. 新增 `adminUploadAsset` 函式（[js/cloud/admin-operations.js](js/cloud/admin-operations.js)），管理員上傳圖片前會先檢查身分與公司權限，通過才會真的上傳，雙重把關。
  3. 拿掉 [edit-chunk-5.js](js/pages/edit/edit-chunk-5.js) 原本的擋板，管理員模式下換照片會自動改用新的 `adminUploadAsset`，一般人自己編輯仍照舊用原本的上傳方式，互不影響。
- **實測驗證**：本機起了瀏覽器實測，確認頁面載入無錯誤、`window.UVACO_CLOUD.adminUploadAsset` 函式已正確存在；因為需要真實登入帳號與真實名片資料，實際「管理員幫別人換照片成功」這一步，還需要你在正式網站上用管理員帳號實際操作一次確認。
- **影響範圍**：只新增權限，不影響一般使用者自己編輯名片的既有功能；企業管理員仍然無法碰到其他公司的名片。
- **快取更新**：`cloud.js` 重新打包，所有引用頁面版本參數升為 `20260717a`；`edit.html` 的 `edit-chunk-5.js` 版本參數同步升為 `20260717a`。

## 2026-07-17（新增：NFC 名片量產管理可以批次輸出印刷貼紙 PDF）

### 新增（批次輸出貼紙 PDF）

- **問題緣起**：印刷廠量產 NFC 名片貼紙時，需要每個人的姓名/職稱/電話/Email/QR Code（正面）跟公司 Logo/名稱（背面），原本只能一張一張複製連結、截 QR Code 圖，沒有辦法批次整理成一份印刷稿。
- **新增功能**：`admin.html` 名片列表新增勾選框（可全選），工具列新增「🖨️ 輸出貼紙PDF」按鈕，勾選多筆名片後一鍵產生一份 PDF，正反面交錯排版（第1頁正面、第2頁背面、第3頁正面…），可直接送印刷廠。
- **技術重點**：專案原本沒有引入任何 PDF 產生工具，也沒有中文字型，這次新增 jsPDF 函式庫（只在按下輸出按鈕時才載入，不拖慢平常開後台的速度）與 Noto Sans TC 中文字型檔（[fonts/NotoSansTC-Regular.ttf](fonts/NotoSansTC-Regular.ttf)，解決 jsPDF 內建字型不支援中文、姓名會變成空白方塊的問題）。
- **實測驗證**：寫了一個獨立的瀏覽器測試頁面，實際產生過一份含中文姓名/職稱/公司名稱＋QR Code 的 PDF 並截圖確認，中文顯示正常、排版跟參考圖比例吻合。
- **影響範圍**：只修改 `admin.html` 一個檔案＋新增一個字型檔案，不影響其他頁面，也不需要修改資料庫。
- **快取更新**：無（`admin.html` 沒有版本參數快取機制）。

## 2026-07-17（優化：「名片管理／訂閱管理」改成獨立分頁籤，不再共用按鈕互相跳轉）

### 修正（後台頁籤互動）

- **問題緣起**：`admin.html` 原本的「💎 訂閱管理」其實是同一顆按鈕在切換：點一下按鈕文字會換成「📋 名片管理」，同時把同一張表格整個抹掉重畫，體驗上像是「跳轉」，容易讓人搞不清楚目前在哪個畫面。
- **修正方式**：改成兩個固定的分頁籤（各自有獨立的表格），點哪個就顯示哪個，按鈕文字不再互換；後續再依需求把這兩個分頁籤搬進最上面的工具列，跟「重新整理」等按鈕放在同一排，並用一致的綠色（目前選中）／灰框（未選中）樣式呈現。
- **影響範圍**：只調整 `admin.html` 的畫面結構與對應 JavaScript，只有超級管理員看得到這兩個分頁籤，一般企業管理員畫面不受影響。
- **快取更新**：無（`admin.html` 沒有版本參數快取機制）。

## 2026-07-08（新增：一鍵開啟「詳細登入診斷」，不用再手動改網址）

### 新增（開發者選項新增 3 個按鈕：開啟/複製/關閉詳細登入診斷）

- **問題緣起**：追查邀請好友卡在 LINE 登入的過程中，一直請對方在網址後面手動加 `&debugAuth=1` 才能看到詳細診斷紀錄，但這個方式對非技術背景的使用者太麻煩，兩次都被誤用成點「診斷連線狀態」按鈕（那個只檢查後端設定，看不到實際卡在哪一步）。
- **新增功能**：在「設定 → 開發者選項」新增 3 個按鈕：
  - **開啟詳細登入診斷**：點一下就好，不用改網址，之後不管重整、或被導去 LINE 官方頁面再導回來，畫面下方都會持續顯示登入過程的即時紀錄。
  - **複製登入診斷紀錄**：一鍵把目前的診斷紀錄複製到剪貼簿，貼給開發人員即可，不用再對著螢幕打字或截圖辨識。
  - **關閉詳細登入診斷**：測試完畢可以關掉，畫面恢復正常。
- **實作方式**：這三個按鈕背後就是原本就有的診斷面板系統（`isAuthDebugEnabled` / `authDebugPanel`），只是新增一個存在 `localStorage` 的開關（`UVACO_FORCE_AUTH_DEBUG`），不用再依賴使用者自己手動改網址參數。
- **實測驗證**：用瀏覽器實際點過這三個按鈕，確認「開啟」會正確顯示面板且內容跟真實登入流程紀錄一致、「複製」會正確寫入剪貼簿、「關閉」會正確清除設定與面板；另外也確認正式站在沒開啟這個開關時，一般使用者不會看到任何診斷面板，不影響一般使用體驗。
- **影響範圍**：只在 `auth.html` 開發者選項新增 3 個按鈕與 `js/pages/auth/main.js` 對應的函式，不影響一般登入流程或其他頁面。
- **快取更新**：`auth.html` 的 `js/pages/auth/main.js` 版本參數升為 `20260708d`；`service-worker.js` 版本升為 `v1.35.3`。

## 2026-07-08（真正找到根因：手機被鎖進「掃碼登入」畫面，但手機沒辦法掃自己的螢幕）

### 修正（手機不再強制走 QR Code 登入；新增手動逃生門連結）

- **問題現象**：上一輪修的「LIFF 自動登入逾時保護」部署後，實測發現朋友的手機根本沒有卡在「正在自動登入…」，而是點了「用 LINE App 開啟」按鈕之後，卡在畫面上沒反應/進不去。
- **真正找到的根因**：LINE 登入有一段「備援機制」——如果切去 LINE App 沒成功，2.2 秒後會自動改用「LINE 網頁版登入」，而這段網頁版登入程式碼裡（[line-liff.js:81](js/cloud/line-liff.js#L81)）寫死用 `initial_amr_display=lineqr` + `switch_amr=false`，意思是「畫面固定顯示掃碼登入、而且不准使用者切換成別的登入方式」。這個設計原本是給**電腦瀏覽器**用的（使用者拿手機掃電腦螢幕上的QR Code），但程式沒有分辨裝置，**手機也被套用一樣的規則**——手機使用者只有手上這一支手機，沒辦法自己掃自己螢幕上的QR，等於被鎖進一個沒有出路的畫面，這才是真正卡住進不去的原因。
- **修正方式**：
  1. 判斷是手機還是電腦（[line-liff.js:58](js/cloud/line-liff.js#L58) 新增 `isMobileUserAgent()`），只有**電腦**才鎖定掃碼登入；**手機改成讓 LINE 自行判斷最適合的登入方式**（且使用者仍可自由切換），不會再被鎖死。
  2. 額外加上一道保險：不管上面的自動判斷準不準，點下「用 LINE App 開啟」後，畫面立刻出現一個「卡住了嗎？點此改用其他方式登入」的手動連結（[auth.html](auth.html)、[main.js](js/pages/auth/main.js)），使用者隨時能自己點著改路走，不用被動等待可能失準的計時器。
  3. 同步修正所有提到「QR Code」的畫面文字，因為手機現在不一定會看到QR Code畫面，文字改成中性的「其他方式登入」，避免文字跟實際畫面對不起來、誤導使用者。
- **跟上一輪的關聯**：上一輪加的「LIFF 自動登入 8 秒逾時保護」仍然保留（防的是另一種情況：LIFF 初始化本身卡住沒回應），這次是額外處理「備援機制本身把手機鎖進不適用畫面」這個更根本的問題，兩者不衝突、互相補強。
- **實測驗證**：用瀏覽器實際模擬手機／電腦兩種 User-Agent，確認手機的登入網址不會帶 `initial_amr_display`/`switch_amr` 參數、電腦則維持原樣不受影響；也實際模擬點擊「用LINE App開啟」與逃生門連結，確認畫面顯示/隱藏、以及呼叫時機都正確，不會重複觸發登入請求。**未能在真實手機+LINE App環境重現原始卡住畫面**，這點如實告知；上述修正是根據程式碼比對LINE官方OAuth參數文件、以及使用者回報「正常人只會帶一支手機」這個關鍵線索推導出的根因，邏輯上能完整解釋回報的現象。
- **影響範圍**：只調整 `js/cloud/line-liff.js` 的登入網址參數邏輯與 `js/pages/auth/main.js` / `auth.html` 的登入頁互動和文字，不影響其他頁面；電腦版登入行為完全不變。
- **快取更新**：`cloud.js` 再次重新打包，所有引用頁面版本參數升為 `20260708c`；`auth.html` 的 `js/pages/auth/main.js` 版本參數同步升為 `20260708c`；`service-worker.js` 版本升為 `v1.35.2`。

## 2026-07-08（修正：邀請好友分享後，卡在 LINE 登入畫面進不去）

### 修正（LIFF 自動登入加上逾時保護，避免永遠卡住）

- **問題現象**：用「設定 → 邀請好友（點擊分享）」把連結分享給朋友後，朋友點開連結卡在 LINE 登入畫面，沒辦法進入名片系統。
- **追查過程**：邀請連結的網址是 `auth.html?ref=推薦人編號&next=edit.html`（[cards-referrals-nfc.js:224](js/cloud/cards-referrals-nfc.js#L224)）。朋友點開後，頁面會自動嘗試「LIFF 自動登入」（[main.js:287](js/pages/auth/main.js#L287)），這會呼叫 LINE 官方原生的登入畫面（`liff.login()`）。
- **確認找到的問題**：整段 LIFF 自動登入流程**完全沒有設定逾時**。對照同一支檔案裡，LINE 一般網頁版登入的網路請求都有包 15 秒逾時保護（[line-liff.js:141](js/cloud/line-liff.js#L141)），但 `liff.init()` / `liff.login()` 這段完全沒有任何逾時機制——如果 LINE App 端卡住沒回應（網路不穩、LINE App 版本問題等），畫面就會「正在自動登入…」卡到天荒地老，使用者不知道其實旁邊的「用 LINE App 開啟」按鈕還能按。
- **修正方式**：幫 LIFF 自動登入包上「最多等 8 秒」的逾時保護（新增 `withTimeout` 輔助函式），超過 8 秒還沒有結果，就自動判定為逾時、跳出明確訊息「自動登入等待逾時，請點下方『用 LINE App 開啟』再試一次」，讓使用者知道要手動按按鈕，不會再讓畫面陷入沒有出路的等待。
- **順便修好的關聯 bug**：`liff.login()` 導回網址原本只帶 `next` 參數、**沒有帶邀請的 `ref` 推薦人編號**，這次一併補上，讓透過邀請連結進來、需要走 LINE 官方登入畫面的朋友，推薦紀錄不會在這個環節被遺漏。
- **這次無法從程式碼 100% 確認的部分（誠實說明）**：好友實際卡住畫面當下，到底是「LIFF 初始化真的卡住等不到回應」（這次修的逾時保護能解決）、還是「LINE 開發者後台的 LIFF Endpoint URL 設定跟實際網址不符」、或是單純網路問題，這三種都會表現成「卡住進不去」但成因不同。已經用瀏覽器實際測試過 `withTimeout` 輔助函式本身的邏輯（逾時會正確 fallback、不會重複觸發、原本的 promise 不會被強制中斷），但沒辦法在真正的 LINE App 環境重現原始問題，所以無法保證這就是唯一根因。
- **後續驗證建議**：如果修正後朋友還是卡住，麻煩請朋友點連結時後面加上 `&debugAuth=1`（例如 `auth.html?ref=xxx&next=edit.html&debugAuth=1`），畫面下方會出現即時診斷面板，把上面顯示的文字截圖回報，就能精準定位卡在哪一步。
- **影響範圍**：只修改 `js/pages/auth/main.js`（新增逾時保護）與 `js/cloud/line-liff.js`（補 ref 參數，需要重新執行 `npm run build:cloud` 打包進 `cloud.js`），沒有動到一般（非邀請）的 LINE 登入流程、Email 登入或其他頁面邏輯；一般登入速度與行為不受影響，只有「逾時」這個新增的例外情況會出現新訊息。
- **快取更新**：`cloud.js` 已重新打包，所有引用它的頁面（`index.html`、`auth.html`、`directory.html`、`admin.html`、`card.html`、`my-card.html`、`company-admin.html`、`edit.html`、`subscription.html`、`settings.html`、`enterprise.html`、`yuyuko.html`）版本參數統一升為 `20260708b`；`auth.html` 的 `js/pages/auth/main.js` 版本參數同步升為 `20260708b`；`service-worker.js` 版本升為 `v1.35.1`。

## 2026-07-08（新增：5 種全新名片主題，共 14 種可選）

### 新增（延續現有卡片版型，只用「更跳脫好分辨」的新配色）

- **需求緣起**：使用者希望在原本 9 種主題之外，再新增 5 種風格完全不重複的主題。第一輪先用文字討論配色概念，確認方向後才動手寫程式，並依使用者回饋調整——不改動卡片版型（圓角卡片、毛玻璃陰影、圓框大頭貼都維持原樣），只把新主題的「顏色」選得更大膽、更容易一眼分辨，避免跟既有 9 種（綠／天藍／金／紫／經典藍／青綠）或彼此之間顏色太接近。
- **新增的 5 種主題**：
  - 主題10「烈焰緋紅」：深黑底 + 紅色強調色
  - 主題11「奶茶桃粉」：暖白底 + 桃紅/洋紅強調色（本次唯一新增的淺色底主題）
  - 主題12「琥珀橙焰」：深黑底 + 橘色強調色
  - 主題13「極簡銀霜」：石墨黑底 + 銀白強調色（唯一不用任何彩色的極簡風主題）
  - 主題14「萊姆電光」：深黑底 + 螢光萊姆綠強調色
- **實作方式**：新增 `theme-10.css` ~ `theme-14.css` 五個主題檔案，並比照既有主題6~9的做法（而非較舊、較笨重的主題1~5全頁覆蓋寫法），統一走 `--ui-accent` 變數系統，維護起來更簡潔。同時把散落在 `js/common/theme-lang.js`、`js/pages/edit/edit-chunk-4.js`、`card.html`、`css/edit-head.css` 裡「主題數量上限是9」的硬編碼判斷，全部同步放寬到14，並在「主題選擇器」畫面新增對應的5張色卡預覽。
- **順便修正的小地方**：新增的淺色底主題（11）套用了跟主題2/7/9同一套「姓名文字強制深色」保護規則（[2026-07-07 的修正](#2026-07-07修正白底主題下姓名文字看不清楚)已經解決過這個問題），避免使用者之前存過淺色姓名字體時，切到這個新淺色主題又重演同樣的「看不清楚」狀況。
- **發現但本次不處理的既有小狀況**：檢查主題選擇邏輯時，順便發現「新手引導精靈（Wizard）」步驟4的主題選擇畫面，其實是一套跟正式9~14種主題完全無關、命名也對不起來的舊資料（例如點下去顯示「經典黑」，但視覺預覽跟正式的「深色主題」對不上）。這個部分現況本來就是這樣、不是這次改壞的，這次沒有動它；如果之後也想一併整理，再麻煩另外提出。
- **實測驗證**：這次有實際架本機網頁伺服器、用瀏覽器工具把 5 個新主題的卡片畫出來看過（不是只看程式碼），並用 WCAG 對比度公式逐一檢查文字/按鈕的顏色是否看得清楚。實測中發現「琥珀橙焰」按鈕文字顏色對比度不夠（3.34，未達建議值4.5），已經當場調深文字顏色（`#7c2d12` → `#431407`）修正到 5.58，其餘4個主題實測對比度都在合格範圍（4.6~19.78）。另外也用程式模擬「點選新主題」的操作，確認10~14號主題都能正確更新畫面與儲存用的主題值，沒有重演之前「選了但沒存到」的問題。
- **影響範圍**：只新增檔案與放寬數字上限，沒有修改任何既有 1~9 號主題的顏色或樣式，姓名/職稱/公司資訊等欄位的儲存邏輯也完全沒動。
- **快取更新**：`common.js` 已重新打包（`js/common/theme-lang.js` 原始碼改動後執行 `npm run build:common`），所有引用 `common.js` 的頁面（`edit.html`、`card.html`、`directory.html`、`settings.html`、`auth.html`、`subscription.html`、`yuyuko.html`）版本參數統一升為 `20260708a`；`edit.html` 的 `css/edit-head.css`、`js/pages/edit/edit-chunk-4.js` 版本參數升為 `20260708a`；`service-worker.js` 版本升為 `v1.35.0`，並把 5 個新主題 CSS 加進離線預先快取清單。

## 2026-07-07（修正：選新主題並儲存後，「我的名片」跟分享連結沒有跟著換主題）

### 修正（選主題時忘了同步更新「儲存要用的主題值」）

- **問題現象**：在「編輯名片」點主題選擇器換成新主題（例如淺色主題），畫面預覽看起來已經換好了，按「儲存」也沒有出現錯誤，但點下方導覽列「我的名片」、或分享給客戶的連結，顯示的還是舊主題，沒有真的換過來。
- **抓到根本原因**：程式裡「畫面上看到的主題」跟「按儲存時真正會送去資料庫的主題」，其實是兩個沒有綁在一起的變數。點主題選擇器時，只更新了畫面預覽用的樣式（[js/pages/edit/edit-chunk-4.js](js/pages/edit/edit-chunk-4.js) 的 `applyCardThemeVisual()`），但另一個專門給「儲存」用的變數 `window.currentCardTheme`，只有在「一開始打開編輯頁、從資料庫讀出舊資料」時被設定過一次（[js/pages/edit/edit-chunk-1.js:264](js/pages/edit/edit-chunk-1.js#L264)），之後不管在畫面上點選幾次新主題都沒有人更新它。所以按「儲存」時（[js/pages/edit/edit-chunk-5.js:41](js/pages/edit/edit-chunk-5.js#L41)），送出去的其實是「打開這頁當下的舊主題」，不是「剛剛畫面上選的新主題」。「我的名片」跟分享連結都是直接讀資料庫，資料庫裡本來就沒真的存到新主題，畫面當然不會換。
- **修正方式**：把「更新畫面預覽」跟「更新儲存要用的主題值」綁在同一個函式裡（`applyCardThemeVisual()`——這個函式本來的設計初衷就是「不管主題是使用者剛選的、還是重新整理後從資料庫讀回的，都要呼叫這裡才會生效」，這次補上遺漏的那一步），之後不管是使用者手動選主題、還是頁面載入時從資料庫回填，「畫面」跟「儲存值」永遠保持同步，不會再各自為政。
- **驗證方式**：搜尋整個專案程式碼，確認 `window.currentCardTheme` 這個變數全專案只有 2 個地方用到（一個寫入、一個讀取），修改前後都逐行追蹤程式邏輯確認資料流向正確；並用 Node.js 對修改後的檔案做語法檢查，確認沒有語法錯誤。**這次驗證是程式碼比對與邏輯追蹤，還沒有實際登入帳號做真人點擊測試**，部署後仍建議照下面驗證清單實際操作一次。
- **影響範圍**：只在 `js/pages/edit/edit-chunk-4.js` 的 `applyCardThemeVisual()` 補上一行同步邏輯，沒有動到儲存/讀取資料庫的其他程式碼，也沒有動到姓名、職稱等其他欄位的邏輯。
- **快取更新**：`edit.html` 的 `js/pages/edit/edit-chunk-4.js` 版本參數升為 `20260707b`，`service-worker.js` 版本升為 `v1.34.14`。

## 2026-07-07（修正編輯名片頁面：白底主題下姓名文字看不清楚）

### 修正（白底主題強制姓名用深色字，避免被舊的自訂淺色蓋過去）

- **問題現象**：在「設定 → 編輯名片 → 主題」，切到「淺色主題（2）」「白底＋經典藍（7）」「白底＋青綠（9）」這三個白底主題時，姓名（例如「邱瑋浚 Stanley」）幾乎變成透明、看不清楚，但下面的職稱文字顯示正常。
- **抓到根本原因**：這個名片系統有一個「姓名字體顏色調色盤」功能，讓使用者自己挑姓名文字顏色，選好的顏色會直接寫死存起來，之後不管換到哪個主題都會照樣套用。如果之前是在深色主題底下選了淺色文字（例如白色），换到白底主題時，這個「寫死的淺色」就會蓋過主題原本該有的深色文字，變成白字疊在白底上，才會幾乎看不見。線上分享出去給別人看的名片頁（`card.html`）本來就有針對這三個白底主題加上「強制深色字」的保護，但「編輯名片」這一頁的預覽（`edit.html`）漏掉了同樣的保護，才只有編輯畫面會出現這個問題。
- **修正方式**：在編輯頁專用的樣式檔案補上跟線上頁面一致的保護規則——只要目前預覽的主題是這三個白底主題之一，姓名文字一律強制使用深色高對比顏色，蓋掉任何殘留的自訂淺色，確保白底一定看得清楚；其餘 6 個深色主題完全不受影響，使用者原本自訂的姓名顏色照舊生效。
- **影響範圍**：只修改 `css/edit-head.css` 新增 3 行主題對應的樣式規則，沒有動到 `card.html`（本來就是好的）、`common.js`、姓名/職稱的儲存邏輯，也沒有動到其他 6 個主題的樣式，不影響既有功能。
- **快取更新**：`edit.html` 的 `css/edit-head.css` 版本參數升為 `20260707a`，`service-worker.js` 版本升為 `v1.34.13`。
- **給使用者的提醒**：如果之後在「職稱」欄位也自訂過淺色字，同樣的狀況理論上也可能發生在職稱上；目前這次只確認並修正了「姓名」欄位，職稱欄位若之後也遇到看不清楚，請再告知一聲即可比照處理。

## 2026-07-06（修正主畫面圖示每次都要重新登入，才能看到自己的名片）

### 修正（新增「不靠手機記憶」的專屬主畫面捷徑）

- **問題現象**：把「我的名片」加到手機主畫面後，第一次登入沒問題，但之後每次點主畫面圖示，都還是顯示「這支手機尚未記住您的名片，請先登入一次」，一直要重複登入，沒辦法像正常 App 一樣點一下直接開啟。
- **抓到根本原因**：原本的設計是「記住手機本機資料（登入紀錄）」，靠這份本機資料認出使用者是誰。但手機系統（尤其 iPhone）對於「加到主畫面」的網頁 App，本來就有可能在某些情況下清掉這份本機資料（跟一般網頁分頁的資料是分開存放、也分開被清除的），一旦被清掉，主畫面圖示就會忘記使用者是誰，變成每次都要重新登入，而且無法從網頁端強制避免手機系統清資料這件事。
- **修正方式**：不再讓主畫面圖示完全依賴「手機本機記住的資料」，改成在「設定」頁新增一個「主畫面捷徑連結」，這個連結裡已經直接寫死自己的專屬名片編號。之後只要刪除舊的「我的名片」捷徑，改用這個新連結重新「加入主畫面」，不管手機本機資料有沒有被清掉，點這個新捷徑都保證能直接看到名片，不會再卡在登入畫面。
- **怎麼換成新捷徑（給使用者的步驟）**：① 打開名片 App，進到「設定」頁；② 點選新出現的「主畫面捷徑連結」項目，會自動複製連結；③ 到 Safari 貼上這個連結並打開；④ 用 Safari 的「加入主畫面」建立新捷徑；⑤ 從主畫面刪除舊的「我的名片」圖示，改用新建立的這個。
- **影響範圍**：新增 `settings.html` 一個項目與 `js/pages/settings/main.js` 兩個函式；`js/pages/my-card/main.js` 新增「網址帶編號就直接開啟」的判斷，不影響原本沒有帶編號時的登入流程與其他頁面。
- **快取更新**：`my-card.html` 的 `js/pages/my-card/main.js` 版本參數升為 `20260706a`，`settings.html` 的 `js/pages/settings/main.js` 版本參數升為 `20260706a`，`service-worker.js` 版本升為 `v1.34.7`。

### 追加修正（實測發現：手機「加入主畫面」會忽略網址上的編號）

- **問題現象**：用上面新的「主畫面捷徑連結」開啟，畫面顯示正常；但用瀏覽器「加入主畫面」建立捷徑後，再點這個新捷徑，還是又跳回要重新登入的畫面。
- **抓到根本原因**：手機（尤其 Android，部分新版 iOS 也會）用「加入主畫面／安裝」建立捷徑時，並不是單純記住當下瀏覽的網址，而是改成永遠去讀 App 資訊清單（`manifest.json`）裡固定寫死的「預設進入頁面」。這個檔案裡原本寫死是 `my-card.html`（沒有帶編號），所以捷徑一旦用這種方式建立，網址上原本帶的名片編號就會被系統忽略、憑空消失，捷徑等於變回最原始、沒有編號的舊版，才會又要重新登入。
- **修正方式**：在 `my-card.html` 載入時，偵測到網址有帶編號，就即時產生一份「進入頁面已經包含這組編號」的專屬 App 資訊清單，取代原本寫死的版本。之後不管手機系統用哪一種方式記住這個捷徑，進入頁面都會保留這組編號，不會再被清空。
- **影響範圍**：只在 `my-card.html` 新增一小段判斷邏輯，不影響其他頁面與既有登入流程。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.8`。
- **給使用者的提醒**：先前已經建立的捷徑（不管新舊）都建議刪除重建一次，確保套用到這次的修正。

### 再次追加修正（實測仍失敗：找到真正被使用者看到的那一頁）

- **問題現象**：套用上一版修正後，實際測試仍然一樣，點新捷徑還是跳回要重新登入。
- **抓到真正原因**：`my-card.html` 判斷到網址帶編號時，幾乎是「零延遲」立刻轉址到 `card.html`，一般人手動點「加入主畫面」需要好幾秒（開分享選單、找選項、確認），這幾秒內畫面早就已經跳到 `card.html`。所以使用者實際按下「加入主畫面」當下，看到、被手機記住的其實是 `card.html` 這一頁，而不是 `my-card.html`——上一版的修正卻只改了 `my-card.html` 這一頁的資訊清單，改錯了頁面，才會沒有效果。
- **修正方式**：把同一套「動態產生帶編號的專屬進入頁面」邏輯，也加到 `card.html` 本身（使用者實際按下加入主畫面時所在的那一頁），這次兩邊都涵蓋到。
- **影響範圍**：只在 `card.html` 新增一小段判斷邏輯，跟 `my-card.html` 是同一種手法，不影響名片顯示或其他既有功能。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.9`。
- **給使用者的提醒**：請再刪除目前的捷徑，重新用「設定」頁的「主畫面捷徑連結」建立一次。

## 2026-07-06（「設定」頁按鈕改成一鍵加入主畫面，不用再手動複製貼上）

### 新增（Android 直接跳出系統安裝視窗；iPhone 仍維持引導教學）

- **需求**：使用者希望「設定」頁的「主畫面捷徑連結」項目，點下去能直接跳出手機「加入主畫面」的功能，而不是先複製連結、再自己去 Safari 貼上開啟。
- **能做到的部分（Android / Chrome 系瀏覽器）**：手機系統本身就有提供「這個網站可以直接跳出安裝視窗」的功能，這次讓「設定」頁預先準備好「已經包含自己名片編號」的安裝資訊，使用者點一下項目，就會直接跳出系統的「加入主畫面」視窗，確認後就完成安裝，不需要再手動複製、貼上、找選單。
- **無法做到的部分（iPhone / Safari）**：老實說明——蘋果基於安全考量，不開放任何網站用程式「自動跳出」加入主畫面的視窗，只能由使用者自己點 Safari 分享鈕手動加入，這是蘋果系統的限制，任何網站都無法繞過。iPhone 使用者點這個項目，維持原本「複製連結 + 顯示操作步驟」的做法。
- **影響範圍**：只修改 `settings.html`（新增一段安裝視窗攔截程式）與 `js/pages/settings/main.js` 的 `copyHomeScreenLink()`，不影響其他頁面。
- **快取更新**：`settings.html` 的 `js/pages/settings/main.js` 版本參數升為 `20260706b`，`service-worker.js` 版本升為 `v1.34.10`。

## 2026-07-06（真正解開 iPhone 一直重複登入的關鍵：iOS 根本不理會動態換掉的 manifest）

### 修正（iPhone 上乾脆移除 manifest 參照，改用蘋果自己更早期的機制）

- **問題現象**：用實機測試（切到真正 Safari、貼上連結、加入主畫面、刪除舊圖示）之後，新捷徑點開還是出現「這支手機尚未記住您的名片」，跟前幾版修正前一模一樣。
- **抓到真正原因**：前兩版修正的假設是「手機安裝捷徑時，只要用程式把 App 資訊清單（manifest.json）動態換成含編號的版本，系統就會採用新內容」——這招在 Android / Chrome 上有效，但實機驗證後發現 **iOS Safari 的「加入主畫面」雖然一樣是照 manifest 裡的固定進入頁面設定，卻不會採用我們用程式動態換掉的內容**，還是抓到伺服器上那份原始、寫死「my-card.html（沒有編號）」的檔案，導致捷徑不管重建幾次，都會回到沒有編號的舊版、又要重新登入。
- **修正方式**：既然這份共用的 manifest 在 iOS 上沒辦法個人化，與其讓 iOS 誤用它，不如偵測到是 iOS 時，直接把 manifest 的參照整個移除。iOS 這時會退回蘋果自己另一套更早期、單純「記住你按下加入主畫面當下的網址」的機制（這套機制跟 manifest 完全無關，長年都是這樣運作），網址上的名片編號就能確實被保留、不會再被清空。Android 因為動態換掉 manifest 有效，維持原本的做法不變。
- **影響範圍**：只修改 `my-card.html`、`card.html` 這兩頁裡「處理主畫面捷徑」的判斷邏輯，不影響圖示、標題、全螢幕模式（這些是另一套跟 manifest 無關的蘋果專屬標籤在控制），也不影響其他頁面。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.11`。
- **給使用者的提醒**：請再刪除目前的捷徑，重新用「設定」頁的按鈕建立一次，這次應該才會是真正修好的版本。

### 再次追加修正（提供實機截圖後才抓到：iOS 連「用程式移除標籤」都不接受）

- **問題現象**：使用者這次提供了完整操作截圖（貼上網址 → 加入主畫面 → 點新捷徑），證實上一版修正仍然無效，新捷徑點開還是出現「這支手機尚未記住您的名片」。
- **抓到真正原因**：上一版的做法是「網頁先正常寫出 manifest 標籤，再用程式即時把它移除」。這次判斷：iOS 很可能是在瀏覽器「一開始收到網頁原始內容」的當下，就已經認定這一頁有 manifest 這份清單了，之後不管用程式怎麼把清單內容換掉、或整個標籤移除，都改變不了 iOS 一開始的認定，才會每次都還是抓去清單裡固定寫死、沒有編號的頁面。
- **修正方式**：這次改成從一開始就不要讓 iOS 看到有 manifest 這回事——用 `document.write`，在瀏覽器「正在讀取網頁」的當下就先判斷是不是 iOS，是的話從頭到尾都不寫出這個標籤（不是先寫出來再移除）。這樣 iOS 就不會誤以為這一頁有專屬的固定進入頁面，會改用蘋果自己另一套「單純記住你按下加入主畫面當下的網址」的機制，網址上的名片編號才能真正被保留下來。同時也把 `my-card.html` 補上跟 `card.html` 一樣的「禁止瀏覽器快取舊版網頁」設定，排除手機讀到舊版內容的可能性。
- **影響範圍**：只修改 `my-card.html`、`card.html` 這兩頁最前面判斷 manifest 標籤的程式碼，不影響圖示、標題、全螢幕模式、其他頁面。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.12`。
- **給使用者的提醒**：請再刪除目前的捷徑，重新用「設定」頁的按鈕建立一次。

## 2026-06-27（修正分享名片給客戶時，背景沒套用自己設定的主題）

### 修正（客戶看到的整個畫面，現在完全照名片主人設定的主題顯示）

- **問題現象**：在手機上設定好「主題」，分享名片連結／NFC 給客戶看，客戶打開後，名片本身的顏色是對的，但背景、頂部與底部欄等周圍的畫面卻沒有跟著變成設定的主題，看起來不一致。
- **抓到原因**：公開名片頁套用的是「已登入使用者瀏覽同事名片」專用的邏輯——這套邏輯故意設計成「卡片顯示對方的主題，但背景保留瀏覽者自己慣用的主題」，是給內部目錄頁瀏覽多位同事名片時用的合理設計。但客戶是透過分享連結／NFC 打開、不是登入後瀏覽同事名片，根本沒有「客戶自己的主題」這種東西，所以背景就變成隨機套用客戶手機上殘留或預設的主題，跟名片主人實際設定的不一致。
- **修正方式**：公開名片頁改用專屬邏輯，不再套用「保留瀏覽者主題」這套規則，整個畫面（背景、頂部欄、底部欄、卡片本身）一律完全照名片主人設定的主題顯示。
- **影響範圍**：只修改 `card.html` 自己的主題套用邏輯，沒有更動共用的 `common.js` / `js/common/theme-lang.js`，所以不影響登入後瀏覽同事名片的目錄頁（那邊保留「背景跟自己、卡片跟對方」的原本設計）。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.6`（`card.html` 本身已設定不快取，會直接讀到最新版）。

## 2026-06-27（修正名片主題重新進入編輯頁後會跑掉）

### 修正（主題現在真正以資料庫存的值為準）

- **問題現象**：在編輯頁設定並儲存了主題，當時看起來是對的；但離開後再次進入編輯頁（或重新整理畫面），畫面顯示的主題卻變回別的、不是剛剛存的那個。
- **抓到原因**：編輯頁的主題顯示其實有兩套各自獨立的程式：一套只認「這支手機瀏覽器本機記住的值（localStorage）」，另一套才是真正讀資料庫存的值。但讀資料庫那一套，原本呼叫的是一個其實不存在的函式（程式碼裡打字打錯/漏接，從頭到尾都沒真正執行過），所以資料庫讀回來的主題從來沒有真正套用到畫面上——畫面永遠只看本機記住的值，跟資料庫沒同步。一旦本機記憶不見（例如清過快取、用別的裝置或瀏覽器、私密瀏覽模式），就會變回看起來「主題跑掉」。
- **修正方式**：把「套用主題到畫面」的程式統一成一份，資料庫讀回名片資料後一定會用這份程式正確套用，並同時更新本機記住的值，確保兩邊永遠一致、以資料庫為準。
- **影響範圍**：只修改 `js/pages/edit/edit-chunk-1.js`、`js/pages/edit/edit-chunk-4.js` 的主題套用邏輯，不影響名片其他資料的編輯與儲存。
- **快取更新**：`edit.html` 的 `edit-chunk-1.js` / `edit-chunk-4.js` 版本參數升為 `20260627a`，`service-worker.js` 版本升為 `v1.34.5`。

## 2026-06-27（修正加入通訊錄的公司住址抓錯成範例地址）

### 修正（隱藏欄位的範例資料不會再跑進聯絡人卡片）

- **問題現象**：名片頁本身顯示的公司住址是對的（高雄市左營區文莊路51號），但點「加入通訊錄」存進手機聯絡人的住址卻是系統內建的範例地址（台北市信義區信義路五段7號）。
- **抓到原因**：名片編輯頁的「服務據點」「通訊住址」「公司住址」是三個獨立、可選擇開關的地址欄位。使用者只填了其中一個（公司住址），沒用到的欄位雖然在畫面上是隱藏的，但儲存資料時，這些「隱藏但沒人改過、還是範例文字」的欄位內容也一起存進了資料庫。名片頁面顯示時有正確把隱藏欄位擋掉，但產生通訊錄檔案的程式沒有檢查欄位是不是被隱藏，所以把這些看不見的範例地址也一起塞進了聯絡人卡片。
- **修正方式**：產生通訊錄檔案前，先把所有「隱藏（沒有啟用）」的公司資訊欄位整段移除，只用畫面上真正顯示出來的內容，跟名片頁面看到的一致。
- **影響範圍**：只修改 `supabase/functions/vcard/index.ts` 的資料整理邏輯，不影響名片頁面顯示，也不需要使用者重新編輯或儲存名片，已立即在所有名片生效（不需要等個別使用者去名片編輯頁重新儲存資料）。
- **已部署且已驗證**：已重新部署到正式環境並確認新版本生效。

## 2026-06-27（修正手機主畫面圖示登入後又要重新登入）

### 修正（PWA 加到主畫面後免重登才真的穩定）

- **抓到根本原因**：手機把「我的名片」加到主畫面後，這個圖示其實是用「另一份獨立的儲存空間」運作，跟一般瀏覽器分頁是分開的。過去登入時若是「先切去 LINE App、再切回來」，手機系統有時會把畫面切回一般瀏覽器、而不是切回主畫面圖示本身，導致登入成功的資料寫到了「另一邊」，主畫面圖示還是讀不到，下次點開又要重新登入一次。
- **修正方式**：偵測到目前是「從主畫面圖示開啟（PWA / standalone 模式）」時，登入一律改用「網頁內 QR Code」方式，不再嘗試切去 LINE App。這樣登入全程都留在同一個畫面、同一份儲存空間裡，登入成功後主畫面圖示才能確實記住，下次點擊就能直接開啟名片，不需要再登入。
- **一般瀏覽器體驗不變**：用一般 Safari / Chrome 分頁開啟登入頁，仍維持「優先嘗試開啟 LINE App，失敗才用 QR Code」，這次只調整「已加到主畫面」這一種情境。
- **影響範圍**：只修改 `js/pages/auth/main.js` 的登入方式判斷邏輯與提示文字，沒有改資料庫、沒有改名片公開瀏覽（NFC／分享連結）的既有行為。
- **快取更新**：`auth.html` 的 `js/pages/auth/main.js` 版本參數升為 `20260627a`，`service-worker.js` 版本升為 `v1.34.4`。

## 2026-06-27（真正修好加入通訊錄沒反應——找到伺服器端的真正病因）

### 修正（前 3 次都修錯方向，這次抓到真正原因）

- **抓到根本原因**：之前 3 次都在改「手機點擊後怎麼觸發」，但實際卡住的地方是伺服器（Supabase Edge Function）回應的檔案標頭寫錯：原本寫的是 `Content-Disposition: inline`（意思是「請瀏覽器直接顯示」），但手機瀏覽器根本沒有能力「顯示」聯絡人檔案格式，所以整個流程會安靜地什麼都不做，畫面停在原地。
- **修正方式**：把這個標頭改成 `Content-Disposition: attachment`（意思是「這是一個檔案，請交給系統處理」），這樣 iPhone Safari 才會跳出「新增聯絡人」預覽卡片，Android Chrome 才會把檔案下載到下載資料夾並跳出通知，點開後正常交給聯絡人 App。
- **影響範圍**：只動到 `supabase/functions/vcard/index.ts` 這一個檔案的標頭設定，沒有改資料庫、沒有改前端按鈕邏輯、沒有改既有分享網址或 NFC 連結。
- **已部署且已驗證**：已用 Supabase CLI 重新部署到正式環境，並用唯讀的 `curl` 確認新版標頭已生效。
- **順手補上一道保護**：重新部署時發現，這個公開端點原本是用「關閉登入驗證」的方式部署的，但專案裡沒有任何設定檔記錄這件事——導致這次重新部署若沒手動加上正確參數，會不小心把登入驗證打開，讓手機完全打不到這個網址（比原本的問題更嚴重）。已新增 `supabase/config.toml`，把「`vcard` 端點不需要登入驗證」這件事寫清楚、固定下來，避免以後任何人重新部署時不小心改回去。

## 2026-06-25（修正加入通訊錄點擊無反應）

### 修正（手機直接開啟 VCF）

- **真實連結點擊**：將「加入通訊錄」從按鈕模擬點擊改成實際 `.vcf` 連結，降低手機瀏覽器擋下程式點擊導致無反應的機率。
- **載入完成即寫入連結**：公開名片資料載入後，會直接把 VCF 網址寫入「加入通訊錄」的 `href`。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.3`，讓手機取得新版點擊流程。

## 2026-06-25（加入通訊錄改用真實 VCF 網址）

### 優化（被分享者同意後存入聯絡簿）

- **真實 VCF 網址**：新增公開 `vcard` Edge Function，支援 `id` 與 `nfc` 參數產生標準 `.vcf` 檔。
- **手機匯入更穩定**：公開名片「加入通訊錄」會優先開啟真實 `.vcf` 網址，讓 iPhone / Android 系統接手新增聯絡人流程。
- **同意後儲存**：被分享者仍需在手機聯絡人畫面確認並按儲存，符合手機隱私限制。
- **安全範圍**：不改資料表、不改 Supabase RLS、不改既有 `card.html?id=...` 或 `card.html?nfc=...` 分享網址。
- **快取更新**：`card.html` 的 `cloud.js` 版本參數升為 `20260625c`，`service-worker.js` 版本升為 `v1.34.2`，確保手機取得新版 VCF 連結 helper。

## 2026-06-25（修正手機通訊錄匯入流程）

### 修正（iPhone / Android 匯入體驗）

- **匯入流程修正**：點「加入通訊錄」時，會先使用手機原生分享 / 匯入選單開啟 `.vcf`，不支援時才下載檔案。
- **LINE 資訊保留**：公開名片若有 LINE 或 LINE 官方帳號連結，VCF 會放入備註與網址資訊；手機通訊錄本身沒有 LINE 專用欄位，因此不會自動加好友。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.1`，讓手機可取得新版匯入流程。

## 2026-06-25（公開名片新增加入手機通訊錄）

### 新功能（客戶一鍵保存聯絡人）

- **加入通訊錄按鈕**：公開名片頁新增「加入通訊錄」功能，朋友或客戶點擊後會產生 `.vcf` 通訊錄檔。
- **跨手機系統**：支援 iPhone / Android 以手機內建流程新增聯絡人；最後是否存入仍由手機系統跳出確認。
- **資料盡量完整**：VCF 會帶入姓名、公司、職稱、電話、Email、網址、電子名片連結、社群連結、地址與備註。
- **安全範圍**：不改 Supabase、不改資料表、不改 NFC 或既有分享網址，客戶公開瀏覽仍不需要 LINE 登入。
- **快取更新**：`service-worker.js` 版本升為 `v1.34.0`。

## 2026-06-25（手機主畫面開啟名片免重登）

### 調整（低風險 PWA 入口）

- **修正 LINE 自動登入閃爍**：手機主畫面/PWA 圖示開啟時，若尚未記住名片 ID，不再自動跳 LINE 登入，改停在穩定提示畫面並提供「登入一次」按鈕。
- **登入頁不再自動啟動 LINE**：`auth.html?next=my-card.html` 會等待使用者手動點「用 LINE App 開啟」，避免 LINE App 與 PWA 反覆跳轉。
- **過期登入也可開公開名片**：若手機仍保留舊 LINE 登入 token，主畫面入口會讀取其中的使用者 ID，直接開公開名片；此 ID 只用於公開瀏覽，不放寬編輯權限。
- **主畫面快速開啟**：使用者已登入並查看自己的名片時，系統會在手機本機記住自己的公開名片 ID。
- **免重複 LINE 登入**：從手機主畫面/PWA 圖示開啟 `my-card.html` 時，若 LINE 登入已過期但本機有記住名片 ID，會直接開公開名片頁 `card.html?id=...`。
- **客戶體驗不變**：客戶掃 NFC 或開分享連結 `card.html?id=...` / `card.html?nfc=...` 仍是公開瀏覽，不需要 LINE 登入。
- **權限不放寬**：一般瀏覽器開 `my-card.html` 仍維持 LINE 登入；編輯名片、設定、通訊錄與後台管理也仍需登入。
- **快取更新**：`my-card.html` 與 `auth.html` 的入口腳本版本升為 `20260625b`，`service-worker.js` 版本升為 `v1.33.1`。

## 2026-06-10（LINE 登入改為 LINE App 優先）

### 維護（低風險登入體驗）

- **LINE App 優先**：手機點「用 LINE App 開啟」時，先嘗試開啟 LIFF URL，讓使用者直接進 LINE App 自動登入。
- **QR Code 備援**：若手機沒有成功開啟 LINE App，約 2 秒後自動改用既有 LINE QR Code 登入；桌機仍維持 QR Code 登入。
- **導向保留**：補上 `liff.state` 的 `next` 讀取，避免從 LINE App 回來後丟失原本要前往的頁面。
- **安全範圍**：不改 Supabase 專案、不改資料表、不改 NFC `card.html?id=...` 或 `card.html?nfc=...` 路徑。
- **快取更新**：`auth.html` 登入資源版本升為 `20260610g`，`service-worker.js` 版本升為 `v1.32.0`。

## 2026-06-10（company-admin.html 重構為完整企業管理中心）

### 重大改版（`company-admin.html`）

整合所有企業管理功能至同一頁面，改為三個頁籤：

| 頁籤 | 功能 |
|------|------|
| 👥 員工管理 | 員工列表、搜尋篩選、新增邀請、編輯資料、停用/啟用、NFC 管理（標記/移交）、匯出 CSV |
| 👮 管理員設定 | 新增/編輯/刪除本公司管理員（原有功能） |
| ⚙️ 公司設定 | 鎖定員工不可編輯的欄位（原在 enterprise.html） |

- **企業管理員**進入後，公司名稱自動帶入並鎖定，只能操作本公司員工及管理員
- **超級管理員**進入後，可管理所有公司資料
- `service-worker.js` 版本升為 `v1.30.0`

---

## 2026-06-10（企業主控台新增「編輯員工資料」功能）

### 新功能（`enterprise.html`）

- **新增「編輯」按鈕**：員工列表的操作欄加入「編輯資料」按鈕（桌機表格與手機卡片均有），點擊後彈出編輯 Modal。
- **編輯員工 Modal**：可修改員工的姓名、職稱、部門、公司、Email、電話。企業管理員的公司欄位自動鎖定為自己的公司。儲存後即時生效並重新載入列表。

### 修復（`js/cloud/admin-operations.js`）

- `adminUpdateCard()` 補上 `department` 欄位（原本沒有同步寫入部門）
- 企業管理員編輯員工時，若員工名片公司尚未填寫（邀請剛建立）不再被擋住
- `cloud.js` 重新打包，`service-worker.js` 版本升為 `v1.29.0`

---

## 2026-06-10（企業管理員可新增本公司管理員）

### 修復（關鍵 BUG 修復）

- **修復「新增失敗：RLS 違規」**：資料庫 `admin_users` 表缺少寫入 RLS 政策，INSERT 被 Supabase 拒絕。已新增 `admin_users_write` 政策，允許管理員寫入。（migration `20260609185635_fix_admin_users_rls_write.sql`）
- **修復企業管理員無法新增管理員**：`upsertAdminUser()` 原本只允許超管呼叫，企業管理員一律被擋。現改為：
  - 企業管理員可新增**本公司管理員**（`target_company` 強制等於自己的公司）
  - 企業管理員不能新增超管（`target_company` 不能為 NULL）
  - 超管不受限制
- **修復企業管理員刪除管理員**：`deleteAdminUser()` 同樣僅允許超管，改為企業管理員可刪除本公司管理員（先查 `target_company` 確認歸屬，不符合則拒絕）。
- `cloud.js` 重新打包，`service-worker.js` 版本升為 `v1.28.0`

---

## 2026-06-10（新增管理員選擇器優化）

### 功能改善（`company-admin.html`）

- **新增管理員改用可搜尋下拉選擇器**：原本需手動貼上 UUID，現在改為搜尋框 + 下拉人員清單，可輸入姓名、公司、職稱即時篩選，選取後顯示確認提示。
- **管理員列表顯示姓名**：原本只顯示 UUID，現在對應 `allCards` 資料顯示員工姓名，一目了然。
- **修正 bug**：`getAdminUsers()` 回傳結果由 `result.admins` 修正為 `result.rows`（與實際 API 一致）。
- **同時載入名片人員**：`reloadData()` 改為同時呼叫 `getAllCardsAdmin()` 取得人員清單供搜尋使用。
- `service-worker.js` CACHE_VERSION 升級為 `v1.24.0`

---

## 2026-06-10（企業管理系統 Phase 1）

### 新功能（企業管理後台）

- **新增 `enterprise.html`（企業主控台）**：企業管理員專屬的員工名片儀表板，可查看全公司員工姓名、職稱、部門、啟用狀態、NFC 狀態、瀏覽次數。支援搜尋、部門/NFC/狀態篩選、批次匯出 CSV（含 UTF-8 BOM 支援 Excel 中文）。
- **員工停用功能**：管理員可一鍵停用（含填寫停用原因）或重新啟用員工名片，獨立於訂閱的 `is_visible`，不影響現有訂閱邏輯。停用後 NFC 網址不變，但名片頁顯示「此名片已停用」提示頁（含公司名稱與官網連結）。
- **NFC 狀態管理**：新增 `nfc_status` 欄位（未綁定 / 已綁定 / 已停用 / 遺失），管理員可透過下拉選單切換狀態，已有 NFC 的名片自動補標 `bound`。
- **NFC 一鍵移交**：離職員工的 NFC 卡可直接移交給指定新員工，解除來源綁定並同步更新目標員工狀態，移交失敗自動回滾。
- **`card.html` 停用提示頁**：名片被管理員停用（`admin_disabled = true`）時，NFC 掃描仍導到同一個網址，但顯示「此名片已停用」頁而非個人資料，避免離職員工資訊外流。

### 資料庫（需在 Supabase 執行 `enterprise-phase1.sql`）

- `cards` 表新增欄位：`admin_disabled`、`admin_disabled_by`、`admin_disabled_at`、`admin_disabled_reason`、`nfc_status`、`department`
- 新增 `trg_sync_nfc_status` trigger：設定 `nfc_card_id` 時自動同步 `nfc_status`
- 現有已綁定 NFC 的名片自動補上 `nfc_status = 'bound'`

### JS 新增函式（`js/cloud/admin-operations.js`）

- `disableEmployeeCard(userId, reason)` — 停用員工名片
- `enableEmployeeCard(userId)` — 重新啟用
- `updateNfcStatus(userId, status)` — 手動更新 NFC 狀態
- `transferNfcCard(fromUserId, toUserId)` — NFC 一鍵移交

### 其他

- `service-worker.js` CACHE_VERSION 升級為 `v1.23.0`，新增 `enterprise.html` 至快取清單

---

## 2026-06-10

### 維護（低風險清理與免密登入）

- **正式環境除錯清理**：移除 `card.html`、`admin.html`、`js/cloud/index.js` 與編輯頁腳本中殘留的本機除錯請求，避免正式使用者瀏覽時多送失敗請求。
- **登入頁體驗**：LINE 登入改為優先顯示 QR Code 掃碼登入，並隱藏帳密登入切換；登入頁文案同步改成「用 LINE App 自動登入或掃 QR Code」，降低使用者忘記 LINE 帳密的問題。
- **登入除錯面板**：`auth.html` 底部除錯面板改為只在本機或網址帶 `?debugAuth=1` 時顯示，正式使用者預設不再看到開發資訊。
- **文件與 404**：README 改為電子名片 V5 說明，保留目前線上路徑 `Electronic-business-card--4` 的提醒，避免誤改已發 NFC 連結；補回 `404.html`，找不到頁面時可回首頁、通訊錄或登入頁。
- **編輯頁拆檔**：將原本集中在 `edit-chunk-2.js` 的大型編輯邏輯依功能拆回 `edit-chunk-2/3/4/5.js`，維持既有載入順序與全域函式名稱，不改名片資料格式。
- **重複檔清理**：刪除未被 `card.html` 載入的 `js/pages/card/card-page.js`，避免名片頁維護時出現兩份邏輯。
- **快取更新**：頁面資源版本統一為 `20260610a`，`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.22.0`，確保部署後使用者可拿到新版檔案。

## 2026-04-23

### 修正（編輯頁聯絡方式刪除按鈕）

- **`js/pages/edit/edit-chunk-1.js`**：刪除按鈕事件改為「先呼叫既有 `deleteContactButton`，若函式不存在就直接走內建刪除流程」，避免出現 `deleteContactButton is not defined` 後無法刪除。
- **除錯追蹤**：補上 `run-3 / H11~H13` 執行期日誌，記錄「啟用備援、刪除前數量、刪除後數量」，方便快速確認部署站行為。
- **快取更新**：`edit.html` 的 `cloud/common/edit-chunk` 與 `edit-head/edit-wizard` 版本參數升級為 `20260423a`，`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.21.9`，避免部署站持續讀到舊版刪除邏輯。
- **核心修復（`saveCard is not defined`）**：將原本被錯位切割的 `edit-chunk-2/3/4/5.js` 重新合併為可執行腳本（主邏輯集中於 `edit-chunk-2.js`），`edit-chunk-3/4/5.js` 改為安全佔位檔，並移除 `wizard.js` 外層 `<script>` 包裝，修正前端語法錯誤連鎖造成的功能遺失。
- **二次快取強制更新**：`edit.html` 腳本與樣式版本再次升級為 `20260423b`，`service-worker.js` 的 `CACHE_VERSION` 升級為 `v1.21.10`，確保用戶端一定拿到本次 chunk 修正版。

## 2026-04-14

### 調整（名片開啟統計：本人開啟也計入）

- **`getCardPublic`（`js/cloud/cards-referrals-nfc.js`）**：啟用 `trackView` 時，本人預覽自己的名片也會寫入 `card_views`，後台「最後開啟／次數」與訪客開啟一併統計。
- **`admin.html`**：姓名下方文案由「最後開啟（他人）」改為「最後開啟」；`cloud.js` 查詢參數改為 `?v=20260414b`。
- **資料庫註解**：`card_view_summaries_admin_rpc.sql` 頂部說明已改為與上述語意一致（RPC 本體無需重跑）。
- **快取**：已執行 `npm run build:cloud` 更新根目錄 `cloud.js`；`service-worker.js` 的 `CACHE_VERSION` 為 `v1.21.8`。

### 新增（後台姓名可直接開啟名片）

- **`admin.html`**：名片管理列表的「姓名」改為可點擊連結，管理員可直接開啟該人的 `card.html?id=<user_id>`，不必再先按「複製連結」。
- **互動樣式**：補上姓名連結的滑過與鍵盤焦點樣式，桌機與手機窄版都更容易辨識可點擊區塊。
- **顯示安全性**：姓名文字先做基本字元轉換，避免特殊符號破壞畫面結構。

### 修正（後台模式切換：名片管理 / 訂閱管理）

- **`admin.html`**：修正切回「📋 名片管理」時可能出現「表頭已回名片、但列表仍停留訂閱資料」的問題。主因是 `reloadData` 內使用未定義的 `list` 導致重載中斷，現在已改為安全使用 `rows` 並正常渲染名片列表。
- **除錯保護**：在 `reloadData` 包裝流程補上錯誤追蹤，若切回名片時重載失敗，會留下可追蹤日誌，方便快速定位問題。
- **持續追查（欄位對不上）**：加上 `reloadData` 內「名片開啟統計 RPC 呼叫前/後/失敗」與 `loadSubscriptions` 入口日誌，用來確認是否因統計 RPC 失敗或非預期流程導致表頭與列資料不同步。

### 移除（設定頁：名片瀏覽統計區塊）

- **settings.html**：已移除「名片瀏覽統計」區塊（`viewStatsItem`）。
- **腳本**：`js/pages/settings/main.js` 已移除 `loadViewStats`、`phraseSinceLastView` 及對 `getCardViewStats` 的呼叫（雲端仍保留 `getCardViewStats` 供後台等用途）。
- **註解**：`cards-referrals-nfc.js` 中 `lastViewedAt` 查詢說明改為供後台或報表使用。
- **快取**：`settings.html` 腳本版本 `?v=20260413f`；`service-worker.js` 的 `CACHE_VERSION` 為 `v1.21.7`。

### 新增（後台名片列表：他人開啟時間與次數）

- **管理表格**：`admin.html` 在每位使用者姓名下方顯示開啟時間與「次數」（初版文案為「最後開啟（他人）」；後續已改為含本人開啟，見同日「調整（名片開啟統計：本人開啟也計入）」）。
- **前端**：`js/cloud/admin-operations.js` 新增 `getCardViewSummariesForAdmin`，呼叫 RPC `get_card_view_summaries_for_admin`；`js/cloud/index.js` 掛載並已執行 `npm run build:cloud` 更新 `cloud.js`。
- **資料庫**：新增腳本 [card_view_summaries_admin_rpc.sql](card_view_summaries_admin_rpc.sql)，請在 Supabase SQL Editor 執行後，後台統計才會有正確數據（未執行前 RPC 失敗時畫面仍顯示次數 0、尚無紀錄）。
- **快取**：`admin.html` 的 `cloud.js` 查詢參數改為 `?v=20260414a`。

## 2026-04-13

### 調整（統一 JSON 為 UTF-8 宣告）

- **Edge Functions**：Stripe、LINE Pay 等支付與核查相關函式的 JSON 回應，`Content-Type` 一律為 `application/json; charset=utf-8`；Google、Apple、LINE 授權相關函式對 Supabase 的 POST 亦同。
- **前端**：`js/cloud/subscription.js`、`subscription.html` 送出 JSON 時同步標明 `charset=utf-8`。
- **雲端模組**：`line-liff.js`、`search-storage.js`、`oauth-google-apple.js` 對 Supabase／外部 API 的 JSON 請求標頭同步加上 `charset=utf-8`。
- **除錯探針**：`js/cloud/index.js` 的 ingest 請求已將 `runId` 設為 `post-fix`；請重新執行 `npm run build:cloud` 以更新根目錄的 `cloud.js`。

### 新增（設定頁：名片最後被瀏覽／多久未被開）

- **瀏覽統計**：`settings.html` 的「名片瀏覽統計」會顯示最後一次被他人瀏覽的時間，以及「多久沒有新的瀏覽」白話說明（中／英）；若尚無紀錄則顯示對應提示。
- **統計資料**：`getCardViewStats` 會多回傳 `lastViewedAt`（全期間最後一筆 `card_views.viewed_at`）。
- **本人不自灌次數**：`getCardPublic` 在開啟追蹤時，若目前登入者就是名片擁有者，不再寫入 `card_views`，避免自己開自己名片灌高瀏覽次數。
- **快取**：`cloud.js`／`common.js` 查詢參數 `?v=20260413d`；`card.html` 與 `settings.html`（含 `js/pages/settings/main.js`）腳本版本同步；`service-worker.js` 的 `CACHE_VERSION` 為 `v1.21.5`。

### 新增（後台減少訂閱）

- **訂閱管理**：`admin.html` 在每筆可操作訂閱新增「減少訂閱」按鈕，管理員可直接輸入要扣除的天數，並沿用同一個彈窗完成操作。
- **扣天數規則**：`js/cloud/subscription.js` 新增 `reduceSubscription`，會先做管理員與公司範圍權限檢查，再扣除天數；若扣完已到期，系統會自動改成停用狀態並隱藏名片。
- **雲端 API**：`js/cloud/index.js` 已掛載 `reduceSubscription`，讓後台頁面可直接呼叫。

### 修正（後台訂閱搜尋）

- **`admin.html`**：在「訂閱管理」模式下，搜尋欄改為篩選 `subscriptionData` 並呼叫 `renderSubscriptionTable`，不再誤用名片用的 `allData` + `renderTable`（避免一輸入搜尋就跳回名片管理表頭與列表）。

### 修正（名片頁 QR、瀏覽紀錄）

- **QR Code**：`card.html` 補齊與 `js/pages/card/card-page.js` 相同的 `loadQRCodeScript`，並將 `showQRCode` 改為 `async`，點選時才動態載入 cdnjs 的 qrcodejs，修正線上 `QRCode is not defined`。
- **card_views**：`recordCardView` trim `card_user_id`；僅在有值時寫入 `referrer` / `user_agent`（並限長度）；`viewed_at` 交由資料庫預設，降低與遠端 schema 不一致時的 400；失敗時 console 顯示較具體的訊息。
- **快取**：`cloud.js` 查詢參數 `?v=20260413d`；`service-worker.js` 的 `CACHE_VERSION` 遞增至 `v1.21.5`。

### 修正（通訊錄腳本編碼）

- **平台通訊錄**：修正 `js/pages/directory/directory-a.js` 與 `directory-b.js` 因 `safeSlug` 被拆成兩個檔案而各自無法解析的問題；並清理註解中因編碼損壞導致的亂碼與語法錯誤。已在 `directory-a.js` 補齊完整 `safeSlug`，移除 `directory-b.js` 開頭重複片段，並重新執行 `npm run build:static` 產出 `cloud.js`。

- **通訊錄列表**：雲端名片搜尋結果（仍排除本人，避免與上方「我的名片」重複）合併「+ 新增好友」寫入 localStorage（directoryFriends）的本機聯絡人；列表顯示電話、Email，並標示為手動儲存（無線上名片預覽）。
- **空狀態說明**：若平台上只有自己一張名片且無本機聯絡人，改為清楚說明原因（非資料消失），透過 emptyHint 與 directory-b.js 的 updateDirectoryResults 搭配。
- **程式結構**：getStoredFriends / setStoredFriends 集中於 directory-a.js（先載入），directory-b.js 移除重複定義；directory.html 腳本版本改為 v=20260413a。
- **預覽按鈕**：修正誤用 Python 格式 `\\U0001f440`（JavaScript 不支援）導致畫面顯示 `U0001f440` 文字而非圖示；改為 ES6 `\\u{1F440}` 以正確顯示眼睛圖示。
- **編碼設定**：新增 `.editorconfig`與 `.vscode/settings.json`，統一以 UTF-8 開啟與儲存檔案，降低 Cursor 或其他編輯器誤判編碼導致的亂碼。
- **除錯清理**：移除 `line-liff.js` 與 `auth/main.js` 對本機 `127.0.0.1:7665` ingest 的追蹤請求；登入頁仍可透過底部面板與 localStorage 查看時間軸。
- **對話框亂碼修正**：`card.html` 與 `js/pages/card/card-page.js` 的「加入主畫面」說明文字，將 `📱`、`⋮` 這類在部分手機原生對話框可能顯示為 `�` 的符號改成純文字（如「三點」），避免開啟新介面提示時出現亂碼。
## 2026-03-24

### 偵錯

- `auth.html` 登入／註冊流程加入執行期追蹤，量測 `bootstrap -> LIFF 自動登入 -> LINE callback code 交換 -> session 判定` 各階段耗時，並在登入頁顯示臨時 timing 面板，供手機端直接觀察慢點位置。

### 清理與部署

- 移除未納入導覽與 Service Worker 的 `debug_jwt.html`（JWT 驗證請改以 Supabase Dashboard 或本機工具處理，避免多一處金鑰相關靜態頁）。
- `CACHE_VERSION` 遞增至 `v1.21.1`；重新執行 `npm run build:static` 產出 `cloud.js` / `common.js`。

### 前端模組化（每個原始 .js 檔 ≤900 行）

- **雲端模組**：新增 `js/cloud/`（常數、HTTP、客戶端、JWT、Session、LINE/LIFF、Google/Apple OAuth、名片與推薦、管理員、搜尋與儲存、合規／匯出／診斷、訂閱與金流），由 `js/cloud/index.js` 組裝 `window.UVACO_CLOUD`；公開 API 補上 `setCustomJwt` 以相容 `auth.html`。
- **部署用 bundle**：根目錄 `cloud.js`、`common.js` 改由 **esbuild** 自模組入口打包為 IIFE（`npm run build:static`），維持既有 `<script src>` 同步載入與內聯 `onclick` 行為。
- **共用模組**：新增 `js/common/`（SW 更新監聽、底部導覽觸覺回饋、Sentry、Analytics、主題／語言、懶載入、圖片壓縮、編輯頁聯絡版型、名片主題初始化）；**修正**無 `.bottom-nav` 時每 500ms 無限 `setTimeout` 重試改為僅在存在導覽列時綁定，並加一次延遲重試。
- **頁面腳本**：`index`、`auth`、`settings`、`my-card` 改為 `type="module"` 入口 `js/pages/*/main.js`；`directory` 拆為 `directory-a.js` / `directory-b.js`（經典腳本、維持全域 `onclick`）；`card.html` 外置 `card-page.js` 並改為**點選 QR 時**才動態載入 qrcodejs；`edit.html` 主樣式抽出為 `css/edit-head.css`、精靈樣式為 `css/edit-wizard.css`，主邏輯拆為 `edit-chunk-1.js`～`5` 與 `wizard.js`、`goto-my-card.js`。
- **Service Worker**：`CACHE_VERSION` 遞增至 `v1.21.0`；`STATIC_ASSETS` 增補 `my-card.html`、`subscription.html`、`admin.html`、`company-admin.html` 與編輯頁 CSS；`isStaticAsset` 納入 `/js/cloud/`、`/js/common/`、`/js/pages/` 與 `/css/edit-` 路徑以利離線快取。
